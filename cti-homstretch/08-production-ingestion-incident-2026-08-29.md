# Production ingestion incident — 2026-08-29

**Status:** Open — degraded ingestion, service online  
**Detected:** 2026-08-29 20:30 UTC run; diagnosis captured 20:48 UTC  
**Scope:** CTI-Hermes production source ingestion

## Impact
The web/API and database remain available, but the last six scheduled ingestion runs are recorded `failed`. Each recent run completed 12 of 14 sources and added documents, while two sources failed. Freshness for those two sources is degraded; successful source documents and existing publications remain present.

## Evidence
- Image: `cti-hermes@sha256:25b6b511409604ddda8195b870c8d9d0cae46feb0a677acfcddd6104f423721d`
- Application: `0.1.0`; Alembic: `0014_hunt_playbooks`
- Latest run `4f8db05e-c29a-5c80-bfe0-464edad4a18`: 20:30:11–20:30:13Z, failed, 14 total / 12 successful / 2 failed / 1840 new documents
- Repeated failures:
  - `google-threat-analysis-group`: `http_error`, HTTP 404
  - `microsoft-security-response-center`: `malformed_xml`, `XML payload could not be parsed`
- Web, scheduler, monitor, backup, and PostgreSQL containers healthy. Worker exited 0 with its reserved-entrypoint message. Monitor restart count is 3 from startup probe failures.
- Liveness, readiness, scheduler heartbeat, and authenticated private version probes returned 200. PostgreSQL connectivity is good; no orphan evidence rows or orphan report-current-version references were found.
- The `/api/v1/ops/last-success` endpoint reports 20:30:13Z because its repository definition treats a failed run with at least one successful source as a partial success; there has been no fully `completed` run in the six-run history.
- Latest encrypted backup metadata is present (3,054,096-byte artifact; metadata SHA-256 recorded; completed 20:30:10Z).
- Host disk 38% used; 55 GiB memory available; FD limit 4096. TLS certificate valid through 2026-11-16.

## Cause assessment
**Primary: high confidence.** Persistent upstream/source-registry incompatibility at the two configured URLs: one returns 404 and one returns non-parseable XML, while the other 12 sources succeed. This is a partial-failure source/configuration issue, not a database or application availability outage.

**Secondary: high confidence.** Unprivileged `nginx -t` cannot read the root-only private key and reports permission denied. Existing root-owned Nginx remains active and ingress works; no reload was attempted. Privileged validation is required before any future reload.

## Actions
Diagnosis only. No restart, migration, deployment, rollback, source edit, credential change, or destructive recovery performed.

## Required follow-up / approvals
1. Review the two upstream endpoints and update `config/sources.json` only through a reviewed, provenance-preserving change; retain failed source-run records.
2. Run privileged Nginx validation and repair the operator validation workflow without weakening key permissions.
3. Any restart, source disablement, deployment, migration, or rollback requires explicit approval and a documented compatible target.

## Current verification update — 2026-08-30 04:18 UTC
- The 02:00 UTC scheduled run also failed with the same two source failures: 12/14 sources completed and 85 new documents were persisted. There are now 7 failed ingestion runs and no fully completed run in the database.
- Web liveness/readiness, private version, last-success, and scheduler-heartbeat probes returned 200; the heartbeat was `2026-08-30T04:17:12Z`. The private Nginx port intentionally returns 404 for public-only paths; the public port returns 200 for `/health/live` and 404 for private paths as configured.
- PostgreSQL is healthy and reachable: Alembic `0014_hunt_playbooks`, 8 active connections of 100, 0 orphan evidence rows, and 0 orphan current report-version references. Disk is 37% used; memory available is 58 GiB; no OOM kills were observed.
- The latest encrypted backup remains present and healthy at 3,054,096 bytes, completed `2026-08-29T20:30:10Z`; it is within the 48-hour freshness threshold. TLS verifies successfully and expires `2026-11-16T14:30:36Z`.
- No restart, migration, deployment, rollback, source edit, credential change, or destructive recovery was performed. The working tree still contains the pre-existing portal CSS modification and this incident record.

## External issue status
- GitHub issue creation was attempted but blocked because `gh auth status` reported no authenticated GitHub host. This repository-local incident record is the durable handoff until an authenticated operator creates the external issue.

## Current verification update — 2026-08-30 05:17 UTC
- The service remains degraded for ingestion, not unavailable: the latest database state still has 7 `failed` ingestion runs; the latest run at 02:00 UTC is `failed`, 14 total / 12 successful / 2 failed / 85 new documents. No fully `completed` run is present.
- Direct upstream probes reproduce the persistent source defect: Google TAG configured RSS URL returns HTTP 404; configured MSRC URL redirects to an HTML page rather than an RSS/XML document. No source registry edit was made.
- Web, scheduler, monitor, and PostgreSQL are running; web, scheduler, monitor, backup, and PostgreSQL report healthy. Worker is intentionally exited 0 with its reserved-entrypoint message. Scheduler heartbeat at 05:17:13Z is fresh. Monitor restart count remains 3 from startup probe failures; no subsequent service restart was performed.
- Application identity is unchanged: version `0.1.0`, commit `fa78a28426a6363be53f7fda225035d5dee171c3`, image `cti-hermes@sha256:25b6b511409604ddda8195b870c8d9d0cae46feb0a677acfcddd6104f423721d`, Alembic `0014_hunt_playbooks`. Containers were created/started around 20:30 UTC on 2026-08-29.
- Database accepts connections (`8/100`), no waiting locks, 0 orphan entity-evidence rows, and 0 orphan report-current-version references. Counts remain 1,895 source documents, 10,338 entity-evidence rows, 169 reports, and 184 publications.
- Latest encrypted backup remains present and verified by matching SHA-256 (`3,054,096` bytes; completed 2026-08-29T20:30:10Z). Host remains healthy: root filesystem 37% used, 58 GiB available memory, no OOM evidence, system FD limit 4096.
- TLS is valid through 2026-11-16. Nginx is active with no restarts, but unprivileged `nginx -t` remains unable to read the root-only private key; privileged validation could not be run because sudo requires a password. Ingress behavior is as designed: port 9443 serves public liveness only, while port 9444 serves readiness/version and denies public-only paths.
- Deployment hygiene finding: the checked-in production Compose file cannot be validated against `/opt/cti-hermes/env/production.env` because that environment file has no `HERMES_SECRET_DIR`; `docker compose ... config --quiet` exits 1. This is configuration/deployment drift, not evidence of current runtime failure, and no deployment or config change was attempted.
- Diagnosis remains: high-confidence persistent upstream/source-registry incompatibility for two enabled feeds; no evidence of database corruption, capacity exhaustion, certificate expiry, provider outage, publication failure, or application availability failure. Diagnosis only; no restart, migration, deployment, rollback, source edit, credential change, or destructive recovery performed.

## Current verification update — 2026-08-30 08:01 UTC
- Incident remains open: the latest persisted run is still `05a4b4f8-33fc-568e-8fc8-77dbd2e770d4`, failed at 02:00:00Z with 12/14 sources successful and 85 new documents; no later run had started at capture. Scheduler heartbeat was fresh at 08:00:44Z.
- Exact configured upstream probes reproduce the two failures: Google TAG `https://blog.google/threat-analysis-group/rss/` returns HTTP 404 HTML; MSRC `https://msrc.microsoft.com/blog/feed` redirects to `https://www.microsoft.com/en-us/msrc/blog` and returns HTTP 200 HTML, not XML. The earlier non-configured FeedBurner probe is not used as evidence.
- Schema-correct PostgreSQL checks: Alembic `0014_hunt_playbooks` equals the local head; 7 failed runs and no completed run; 8/100 connections; zero waiting locks; zero non-null orphan entity-evidence references; zero orphan report-current-version references. The 1,529 active evidence rows previously surfaced by a broad query have `source_document_id=NULL`, not broken references, and the validated FK remains intact.
- Runtime remains healthy: web, scheduler, monitor, backup, and PostgreSQL healthy; worker intentionally exited 0; monitor restart count 3 from startup probe ordering; no service crash/OOM evidence. Host remains at 37% disk use with 58 GiB available memory and FD limit 4096.
- Backup `/backups/hermes-20260829T203010Z.dump.enc` is 3,054,096 bytes and its computed SHA-256 matches metadata `c7d7bbdcd269479e459bc659dc2015f94e7a36d77fc084f5348ecb28ae23f00d`; certificate remains valid through 2026-11-16. No restore rehearsal was run.
- Supplied matrix target returns the expected private readiness response (HTTP 200); the unauthenticated/incorrect-header operations probes returned 404 and are not treated as an application failure. This does not explain ingestion failures. Unprivileged `nginx -t` still cannot read the root-only key; no reload attempted. Production Compose validation still fails because `/opt/cti-hermes/env/production.env` omits `HERMES_SECRET_DIR`; running image remains mutable `cti-hermes:local` with ID `sha256:25b6b511409604ddda8195b870c8d9d0cae46feb0a677acfcddd6104f423721d`.
- No action beyond read-only diagnosis and this incident-record update. GitHub issue creation remains blocked: `gh auth status` reports no authenticated host. No approval exists for source repair, proxy/config change, deployment, migration, restart, rollback, restore, or credential operation.

## Current verification update — 2026-08-30 08:47 UTC
- Incident is still active as degraded ingestion, not an availability outage. The latest persisted run remains `05a4b4f8-33fc-568e-8fc8-77dbd2e770d4` (`failed`, 02:00:00Z–02:00:01Z), with 14 total / 12 successful / 2 failed / 85 new documents. Seven failed runs are recorded and no fully `completed` run exists; the latest partial-success interpretation remains application behavior.
- Exact configured feed probes reproduce the same defects: Google TAG URL returns HTTP 404 HTML (227,758 bytes); MSRC URL redirects to `https://www.microsoft.com/en-us/msrc/blog` and returns HTTP 200 HTML (94,397 bytes), not RSS/XML. The other source-run rows in the latest run are completed, including expected 200/304 responses.
- Runtime state remains healthy: web, scheduler, monitor, backup, and PostgreSQL are running and healthy; worker is intentionally exited 0 with its reserved-entrypoint behavior. Restart counts are web 0, scheduler 0, backup 0, PostgreSQL 0, monitor 3 (startup probe ordering), worker 0. Scheduler heartbeat is fresh at `2026-08-30T08:47:14Z`.
- Private readiness through the supplied TLS endpoint returned HTTP 200 with configuration/database `ok`; public liveness returned HTTP 200. Authenticated operational paths on the supplied 9444 proxy returned 404, consistent with the proxy route/hostname split and not evidence of application failure. TLS verification succeeds for `matrix-1.taild27e3c.ts.net`, Let's Encrypt issuer, expiry `2026-11-16T14:30:36Z`.
- PostgreSQL connectivity is good: Alembic `0014_hunt_playbooks`, 8/100 connections, 0 waiting locks, and 0 orphan entity-evidence or report-current-version references. No failed/pending migration evidence was found. Host resource state is healthy: root 38% used, 58 GiB available memory, FD limit 4096, no kernel OOM evidence; container memory remains low.
- Latest encrypted backup is `/backups/hermes-20260829T203010Z.dump.enc`, 3,054,096 bytes, metadata and computed SHA-256 match (`c7d7bbdcd269479e459bc659dc2015f94e7a36d77fc084f5348ecb28ae23f00d`), completed `2026-08-29T20:30:10Z`; no restore rehearsal was run. Production Nginx is active with zero restarts and ingress works, but unprivileged `nginx -t` still fails on the root-only private key; no reload was attempted.
- Deployment/config evidence is unchanged: running image is mutable `cti-hermes:local` with image ID `sha256:25b6b511409604ddda8195b870c8d9d0cae46feb0a677acfcddd6104f423721d`; containers were created around `2026-08-29T20:30Z`. `/opt/cti-hermes/env/production.env` was last modified `2026-08-25 17:20:29-05:00` and omits `HERMES_SECRET_DIR`, so checked-in production Compose validation remains blocked. No deployment-state record was present, and no approved compatible rollback target was established.
- Diagnosis remains high-confidence source/configuration incompatibility affecting exactly two enabled feeds. No evidence supports web, proxy availability, worker crash, scheduler failure, database corruption, disk/memory/FD exhaustion, certificate expiry, backup failure, provider-wide outage, publication failure, or deployment as the primary cause. No restart, migration, deployment, rollback, restore, source edit, credential change, or destructive recovery was performed.

## Current verification update — 2026-08-30 09:34 UTC
- Incident remains active as partial-failure ingestion, not a service outage. Latest run `05a4b4f8-33fc-568e-8fc8-77dbd2e770d4` is still `failed` (02:00:00–02:00:01Z), 14 total / 12 successful / 2 failed / 85 new documents; seven failed runs are recorded and no fully `completed` run exists.
- Exact configured upstream probes still reproduce Google TAG HTTP 404 HTML and MSRC redirect/HTML instead of RSS/XML. This remains the primary cause with high confidence; no source configuration was changed.
- Runtime state: web, scheduler, monitor, backup, and PostgreSQL are running/healthy; worker exited 0 by design. Restart counts remain web 0, scheduler 0, backup 0, PostgreSQL 0, monitor 3 startup restarts, worker 0. Scheduler heartbeat is fresh (`2026-08-30T09:16:45Z` at capture).
- Supplied private endpoint `/health/ready` and `/version` returned 200; `/health/live` returned 404 by the intentional private-port routing split. However, `/api/v1/ops/version`, `/api/v1/ops/last-success`, and `/api/v1/ops/scheduler-heartbeat` also returned application 404, including direct loopback probes. This is a monitoring/observability deployment mismatch requiring a reviewed fix; it does not explain the source failures and no restart/reload was attempted.
- PostgreSQL: Alembic `0014_hunt_playbooks` (local migration head), 8/100 connections, 0 waiting locks; 1,895 source documents, 10,551 entity-evidence rows, 178 reports, 193 publications, 0 non-null orphan evidence references, and 0 orphan current report-version references. No failed/pending migration or corruption evidence was found. The 1,592 evidence rows with `source_document_id=NULL` are valid nullable provenance records, not orphans.
- Backup verification: `/backups/hermes-20260829T203010Z.dump.enc`, 3,054,096 bytes; computed SHA-256 `c7d7bbdcd269479e459bc659dc2015f94e7a36d77fc084f5348ecb28ae23f00d` matches metadata. No restore rehearsal was run. TLS chain verifies successfully for `matrix-1.taild27e3c.ts.net`, expiry `2026-11-16T14:30:36Z`.
- Host resources remain healthy: root filesystem 38% used, 58 GiB available memory, system FD limit 4096, no OOM kills; Nginx active with zero restarts. Compose validation remains blocked by missing `HERMES_SECRET_DIR` in `/opt/cti-hermes/env/production.env`; running image is mutable `cti-hermes:local` ID `sha256:25b6b511409604ddda8195b870c8d9d0cae46feb0a677acfcddd6104f423721d`. No approved compatible rollback target or deployment-state record was found.
- Actions: read-only diagnosis and this incident-record update only. GitHub issue creation remains blocked because `gh auth status` reports no authenticated GitHub host. No restart, migration, deployment, rollback, restore, source edit, credential change, or destructive recovery was performed. Required approvals: reviewed source-registry repair, observability/proxy deployment fix, any deployment or migration, and any rollback/restore.

- Actions: read-only diagnosis and this incident-record update only. GitHub issue creation remains blocked because `gh auth status` reports no authenticated GitHub host. No restart, migration, deployment, rollback, restore, source edit, credential change, or destructive recovery was performed. Required approvals: reviewed source-registry repair, observability/proxy deployment fix, any deployment or migration, and any rollback/restore.
## Current verification update — 2026-08-30 10:02 UTC
- Incident remains active as degraded/partial-failure ingestion, not an availability outage. Database state is unchanged: 7 failed ingestion runs, 0 completed runs; latest run `05a4b4f8-33fc-568e-8fc8-77dbd2e770d4` completed 02:00:01Z with 14 total / 12 successful / 2 failed / 85 new documents. No later run had started at capture; scheduler heartbeat was fresh at `2026-08-30T10:01:45Z`.
- Exact configured upstream probes still reproduce the source defects: Google TAG returns HTTP 404 with `text/html` (227,758 bytes); MSRC returns HTTP 301 to `https://www.microsoft.com/en-us/msrc/blog`, then HTTP 200 `text/html` (94,397 bytes), not RSS/XML. Latest source-run rows identify `http_error`/404 and `malformed_xml`; the other 12 sources completed.
- Runtime is healthy: web, scheduler, monitor, backup, and PostgreSQL are running and healthy; worker exited 0 with its intentional reserved-entrypoint message. Restart counts are web 0, scheduler 0, backup 0, PostgreSQL 0, monitor 3 startup-probe restarts, worker 0. Private readiness and version plus public liveness each returned HTTP 200. Nginx is active with zero restarts; TLS verification succeeds for `matrix-1.taild27e3c.ts.net`, Let's Encrypt, expiry `2026-11-16T14:30:36Z`.
- PostgreSQL accepts connections (`8/100`), 0 waiting active locks, Alembic `0014_hunt_playbooks` equals the local migration head, and no pending/failed migration or integrity evidence was found. Counts are 1,895 source documents, 10,551 entity-evidence rows (1,592 valid nullable-provenance rows), 178 reports, and 193 publications; orphan entity-evidence and current-report-version references are both 0.
- Host/container capacity is healthy: root filesystem 38% used, 58 GiB available memory, FD limit 4096, low container memory, and no kernel OOM evidence. Latest encrypted backup `/backups/hermes-20260829T203010Z.dump.enc` is 3,054,096 bytes with metadata SHA-256 match `c7d7bbdcd269479e459bc659dc2015f94e7a36d77fc084f5348ecb28ae23f00d`, completed `2026-08-29T20:30:10Z`; no restore rehearsal was run.
- Deployment/config findings persist: running image is mutable `cti-hermes:local`, image ID `sha256:25b6b511409604ddda8195b870c8d9d0cae46feb0a677acfcddd6104f423721d`, application `0.1.0`, commit `fa78a28426a6363be53f7fda225035d5dee171c3`; checked-in production Compose validation exits 1 because `/opt/cti-hermes/env/production.env` lacks required `HERMES_SECRET_DIR`. Environment file mtime remains 2026-08-25 17:20:29 -0500. No approved compatible rollback target or deployment record was found.
- Cause confidence remains high: persistent incompatibility/staleness of exactly two enabled upstream feed URLs. There is no evidence that web, proxy availability, worker crash, scheduler failure, database, disk, memory, file descriptors, certificate, backup, provider-wide outage, publication, or deployment caused the ingestion failure. No restart, migration, deployment, rollback, restore, source edit, credential change, or destructive recovery was performed. Incident record updated as the durable local incident; external issue creation remains blocked by unauthenticated `gh`.

## Current verification update — 2026-08-30 10:18 UTC
- Incident remains active as degraded/partial-failure ingestion, not an availability outage. The latest persisted run remains `05a4b4f8-33fc-568e-8fc8-77dbd2e770d4` (`failed`, `02:00:00.103778Z`–`02:00:01.918747Z`), with 14 total / 12 successful / 2 failed / 85 new documents. Database status is 7 failed runs and 0 completed runs.
- Exact configured upstream probes reproduce the two failures: Google TAG returns HTTP 404 `text/html` (227,758 bytes); MSRC redirects 301 to `https://www.microsoft.com/en-us/msrc/blog` and returns HTTP 200 `text/html` (94,397 bytes), not RSS/XML. The other 12 source-run rows in the latest run are completed (200 or 304). This is the primary cause with high confidence.
- Runtime is healthy: web, scheduler, monitor, backup, and PostgreSQL are running/healthy; worker exited 0 with its intentional reserved-entrypoint behavior. Restart counts are web 0, scheduler 0, backup 0, PostgreSQL 0, monitor 3 startup-probe restarts, worker 0; no OOM kills. Scheduler heartbeat remains fresh; local liveness, readiness, and version returned HTTP 200. The unauthenticated local operations paths return application 404, consistent with the private/authenticated route boundary; authenticated monitor probes in web logs return HTTP 200.
- PostgreSQL accepts connections (`8/100`), reports 0 waiting active locks, and is at Alembic `0014_hunt_playbooks`, matching the local head. Counts: 1,895 source documents, 10,551 entity-evidence rows, 178 reports, 193 publications, and 0 orphan non-null entity-evidence references / 0 orphan current-report-version references. No pending/failed migration or integrity evidence was found.
- Host capacity is healthy: root filesystem 38% used, 304 GiB available, 58 GiB memory available, system FD limit 4096; container memory is low. Nginx is active with 0 restarts and ingress works. Unprivileged `nginx -t` still fails because `/etc/nginx/ssl/tailscale/hermes.key` is root-only; certificate verification succeeds for `matrix-1.taild27e3c.ts.net`, Let's Encrypt, expiry `2026-11-16T14:30:36Z`. No reload was attempted.
- Latest encrypted backup `/backups/hermes-20260829T203010Z.dump.enc` is present, 3,054,096 bytes, with computed SHA-256 matching metadata (`c7d7bbdcd269479e459bc659dc2015f94e7a36d77fc084f5348ecb28ae23f00d`); completed 2026-08-29T20:30:10Z. No restore rehearsal was run.
- Running application containers use mutable `cti-hermes:local` with image ID `sha256:25b6b511409604ddda8195b870c8d9d0cae46feb0a677acfcddd6104f423721d`; application version is `0.1.0`, repository HEAD is `fa78a28426a6363be53f7fda225035d5dee171c3`. The checked-in production Compose validation remains blocked because `/opt/cti-hermes/env/production.env` omits required `HERMES_SECRET_DIR`; that file was last modified `2026-08-25 17:20:29 -0500`. No deployment-state record or approved compatible rollback target was found.
- Actions: read-only diagnosis and this incident-record update only. No restart, migration, deployment, rollback, restore, source edit, credential change, or destructive recovery was performed. GitHub issue creation remains blocked because `gh auth status` reports no authenticated GitHub host. Required approvals: reviewed source-registry repair, observability/proxy/configuration repair, any deployment or migration, and any rollback/restore.

## Current verification update — 2026-08-30 10:48 UTC
- Incident remains active as degraded/partial-failure ingestion, not an availability outage. The database has 7 `failed` ingestion runs and 0 `completed` runs. Latest run `05a4b4f8-33fc-568e-8fc8-77dbd2e770d4` remains `failed` (02:00:00.103778Z–02:00:01.918747Z), 14 total / 12 successful / 2 failed / 85 new documents; no later scheduled run has started.
- Exact configured upstream failures remain reproducible: Google TAG `https://blog.google/threat-analysis-group/rss/` returns HTTP 404 `text/html` (227,758 bytes); MSRC `https://msrc.microsoft.com/blog/feed` returns HTTP 301 to `https://www.microsoft.com/en-us/msrc/blog`, then HTTP 200 `text/html` (94,397 bytes), not RSS/XML. Latest source-run rows identify `http_error`/404 and `malformed_xml`; the other 12 sources completed. Primary cause confidence remains high: stale/incompatible enabled source endpoints.
- Container state is stable: web, scheduler, backup, monitor, and PostgreSQL are running and healthy with restart counts 0, 0, 0, 3, and 0 respectively; worker is intentionally exited 0 with its reserved-entrypoint message (its exited/unhealthy health state is expected for this reserved service). No OOM evidence. Scheduler heartbeat was `2026-08-30T10:48:15Z`.
- Correctly routed probes over the supplied TLS endpoint returned private `/health/ready` HTTP 200 (`configuration=ok`, `database=ok`) and `/version` HTTP 200 (`0.1.0`); public `/health/live` on 9443 returned HTTP 200. Operations paths on 9444 returned application 404 because this proxy surface does not expose those routes, while local authenticated monitor probes in web logs return 200. Nginx is active with 0 restarts and ingress works. Unprivileged `nginx -t` still fails because `/etc/nginx/ssl/tailscale/hermes.key` is root-only; no reload was attempted. Certificate verification succeeds for `matrix-1.taild27e3c.ts.net`, Let's Encrypt issuer, expiry `2026-11-16T14:30:36Z`.
- PostgreSQL accepts connections (`pg_isready: accepting connections`), 8/100 connections, 0 waiting active locks, and Alembic `0014_hunt_playbooks` matches the local migration head. Counts remain 1,895 source documents, 10,551 entity-evidence rows, 178 reports, 193 report versions, and 193 publications; orphan non-null entity-evidence references and orphan current-report-version references are both 0. No pending/failed migration or data-corruption evidence was found. Recent PostgreSQL `ERROR` entries are malformed read-only diagnostic SQL probes, not application migration/write failures.
- Host capacity remains healthy: root filesystem 38% used with 304 GiB available, 58 GiB memory available, FD limit 4096, load 0.14/0.10/0.09, and no kernel OOM evidence. Latest encrypted backup `/backups/hermes-20260829T203010Z.dump.enc` is 3,054,096 bytes with metadata SHA-256 match `c7d7bbdcd269479e459bc659dc2015f94e7a36d77fc084f5348ecb28ae23f00d`, completed `2026-08-29T20:30:10Z`; no restore rehearsal was run.
- Release/config identity is unchanged: application `0.1.0`, repository HEAD `fa78a28426a6363be53f7fda225035d5dee171c3`, mutable image `cti-hermes:local` / digest `sha256:25b6b511409604ddda8195b870c8d9d0cae46feb0a677acfcddd6104f423721d`, containers created/started around `2026-08-29T20:30Z`. `/opt/cti-hermes/env/production.env` remains mtime `2026-08-25 17:20:29 -0500`; checked-in Compose validation is blocked because required `HERMES_SECRET_DIR` and immutable `HERMES_IMAGE` are absent from the environment. No deployment record or approved compatible rollback target was found.
- No restart or other recovery action was justified or performed: source failures are deterministic, data integrity is not in question, and runtime is healthy. GitHub issue creation remains blocked by unauthenticated `gh`. Required approvals remain reviewed source-registry repair, observability/proxy/configuration repair, any deployment or migration, and any rollback/restore.

## Current verification update — 2026-08-30 11:02 UTC
- Incident remains active as degraded/partial-failure ingestion, not an availability outage. PostgreSQL still records 7 `failed` ingestion runs and 0 `completed` runs. Latest run `05a4b4f8-33fc-568e-8fc8-77dbd2e770d4` remains `failed`, scheduled `02:00:00Z`, completed `02:00:01.918747Z`, with 14 total / 12 successful / 2 failed / 85 new documents. No later ingestion run exists at capture time.
- The exact configured upstream defects remain reproducible at 11:01 UTC: Google TAG returns HTTP/2 404 `text/html` (227,758 bytes); MSRC returns HTTP/2 301 to `https://www.microsoft.com/en-us/msrc/blog`, then HTTP/2 200 `text/html` (94,397 bytes), not RSS/XML. The latest `source_run` rows classify these as `http_error`/HTTP 404 and `malformed_xml`; all other 12 enabled sources completed. Primary cause confidence remains high: stale/incompatible source endpoints.
- Runtime is stable: web, scheduler, backup, monitor, and PostgreSQL are running and healthy; worker is intentionally exited 0 with the reserved-entrypoint message and `restart: no`. Restart counts are web 0, scheduler 0, backup 0, PostgreSQL 0, monitor 3 (startup-probe ordering), worker 0. Docker events from 10:45–11:03 UTC contain healthcheck/diagnostic execs only, with no container restart or crash events. Scheduler heartbeat was `2026-08-30T11:01:45Z` (mode 644, 21 bytes).
- Correctly routed TLS probes returned private `/health/ready` HTTP 200 with `configuration=ok,database=ok`, `/version` HTTP 200 with `0.1.0`, and public `/health/live` HTTP 200. Local unauthenticated operations paths returned 404, consistent with the private/authenticated route boundary; web logs show authenticated monitor operations probes returning 200. Nginx is active with zero restarts and ingress works. Certificate verification succeeded for `matrix-1.taild27e3c.ts.net`, Let's Encrypt YE1, valid `2026-08-18` through `2026-11-16 14:30:36Z`.
- PostgreSQL 16.14 accepts connections; Alembic revision is `0014_hunt_playbooks` and matches the local migration head. There are 8/100 connections, no waiting active locks, and no pending/failed migration evidence. Counts are 1,895 source documents, 91 raw artifacts, 10,551 entity-evidence rows, 3,522 indicators, 178 reports, 193 report versions, and 193 publications. Orphan non-NULL entity-evidence references and orphan current-report-version references are both 0. Database logs contain prior malformed read-only diagnostic probes, not application migration/write failures.
- Host/container capacity is healthy: root filesystem 38% used with 304 GiB available; 58 GiB available memory; load `0.16 0.11 0.09`; system FD limit 4096 and `/proc/sys/fs/file-nr` reports `2752` allocated files. Container memory remains low. Kernel `dmesg` OOM verification was unavailable because the diagnostic user lacks permission to read the kernel buffer; no OOM signal was observed in container state/logs.
- Latest encrypted backup remains `/backups/hermes-20260829T203010Z.dump.enc`, mode 600, 3,054,096 bytes, completed `2026-08-29T20:30:10Z`; recomputed SHA-256 matches metadata: `c7d7bbdcd269479e459bc659dc2015f94e7a36d77fc084f5348ecb28ae23f00d`. It is within the 48-hour freshness window. No restore rehearsal was run and no backup was altered.
- Release/config identity is unchanged: repository `git@github.com:mrcoggsworth/threat-intel-agent.git`, branch `main`, HEAD `fa78a28426a6363be53f7fda225035d5dee171c3`, application `0.1.0`, mutable image `cti-hermes:local` with ID/digest `sha256:25b6b511409604ddda8195b870c8d9d0cae46feb0a677acfcddd6104f423721d`. Application containers were created/started around `2026-08-29T20:30Z`; PostgreSQL started `2026-08-26T23:20Z`. Production env mtime remains `2026-08-25 17:20:29 -0500`; checked-in Compose validation exits 1 because `/opt/cti-hermes/env/production.env` omits required `HERMES_SECRET_DIR`. No deployment record or approved compatible immutable rollback target was found.
- Working tree preflight remains non-clean but unchanged by diagnosis: tracked `src/hermes_cti/portal/static/portal.css` modification and untracked `cti-homstretch/08-production-ingestion-incident-2026-08-29.md` were pre-existing. No restart, migration, deployment, rollback, restore, publication mutation, source edit, credential change, Docker prune, volume deletion, or publication mutation was performed. GitHub issue creation remains blocked because `gh auth status` reports no authenticated host; this repository-local incident record is the durable maintenance handoff.

## Current verification update — 2026-08-30 08:02 CDT (13:02 UTC)
- Incident remains active as degraded/partial-failure ingestion, not an availability outage. Database records 7 failed ingestion runs and 0 completed runs; latest run `05a4b4f8-33fc-568e-8fc8-77dbd2e770d4` remains failed (02:00:00Z–02:00:01.918747Z), 14 total / 12 successful / 2 failed / 85 new documents.
- Exact configured upstream probes still reproduce the primary defect: Google TAG returns HTTP/2 404 `text/html` (227,758 bytes); MSRC returns HTTP/2 301 to `https://www.microsoft.com/en-us/msrc/blog` and then an HTML response (94,397 bytes), not RSS/XML. Source-run rows classify these as `http_error`/404 and `malformed_xml`; the other 12 sources completed. Cause confidence remains high: stale/incompatible enabled feed endpoints.
- Container state is stable: web, scheduler, monitor, backup, and PostgreSQL are running healthy; worker is intentionally exited 0 with the reserved-entrypoint message. Restart counts are web 0, scheduler 0, backup 0, PostgreSQL 0, monitor 3 (startup probe ordering), worker 0. Docker events in the last two hours show healthcheck/diagnostic execs only, with no restart or crash events.
- Correctly routed service probes returned private `/health/ready` 200 and `/version` 200, and public `/health/live` 200. PostgreSQL 16.14 accepts connections; 8/100 connections, 0 waiting active locks, Alembic `0014_hunt_playbooks` matches local head, and integrity checks remain 0 orphan evidence references / 0 orphan current report-version references. Counts: 1,895 source documents, 91 raw artifacts, 10,551 entity-evidence rows, 3,522 indicators, 178 reports, 193 report versions, 193 publications.
- Host capacity is healthy: root filesystem 38% used / 304 GiB available, 58 GiB available memory, system FD limit 4096, file-nr 3,840 allocated, no observed OOM signal. Nginx is active with zero restarts and ingress works. Unprivileged `nginx -t` still fails because the root-only `/etc/nginx/ssl/tailscale/hermes.key` cannot be read; no reload attempted. TLS verification succeeds for `matrix-1.taild27e3c.ts.net`, Let's Encrypt YE1, expiry `2026-11-16T14:30:36Z`.
- Latest encrypted backup `/backups/hermes-20260829T203010Z.dump.enc` is present, mode 600, 3,054,096 bytes; metadata and recomputed SHA-256 match (`c7d7bbdcd269479e459bc659dc2015f94e7a36d77fc084f5348ecb28ae23f00d`), completed 2026-08-29T20:30:10Z. No restore rehearsal or backup mutation was performed.
- Release/config identity is unchanged: repository HEAD `fa78a28426a6363be53f7fda225035d5dee171c3`, application `0.1.0`, mutable `cti-hermes:local` image ID `sha256:25b6b511409604dda8195b870c8d9d0cae46feb0a677acfcddd6104f423721d`; containers started around 20:30Z on 2026-08-29. Checked-in Compose validation still exits 1 because `/opt/cti-hermes/env/production.env` omits required `HERMES_SECRET_DIR`; env mtime remains 2026-08-25 17:20:29 -0500. No deployment record or approved compatible rollback target was found.
- Action: read-only diagnosis and this local incident-record update only. No restart, migration, deployment, rollback, restore, source edit, credential change, or destructive recovery. GitHub issue creation remains blocked by unauthenticated `gh`. Required approvals remain reviewed source-registry repair, observability/proxy/configuration repair, any deployment or migration, and any rollback/restore.

## Current verification update — 2026-08-30 08:17 CDT (13:17 UTC)
- Incident remains active as degraded/partial-failure ingestion, not an availability outage. PostgreSQL still records 7 failed ingestion runs and 0 completed runs; latest run `05a4b4f8-33fc-568e-8fc8-77dbd2e770d4` is failed, scheduled 02:00:00Z and completed 02:00:01.918747Z, with 14 total / 12 successful / 2 failed / 85 new documents. No successful run is available to report.
- Exact configured upstream probes at 13:17Z reproduce the primary failures: Google TAG returns HTTP/2 404 `text/html` (227,758 bytes); MSRC returns HTTP/2 301 to `https://www.microsoft.com/en-us/msrc/blog`, then HTTP/2 200 HTML (94,397 bytes), not RSS/XML. Database source-run rows classify them as `http_error`/404 and `malformed_xml`; all other 12 sources completed. Cause confidence remains high: stale/incompatible enabled source endpoints.
- Runtime is stable: web, scheduler, monitor, backup, and PostgreSQL are running healthy; worker exited 0 with its intentional reserved-entrypoint behavior and is expected to be unhealthy under `restart: no`. Restart counts: web 0, scheduler 0, backup 0, PostgreSQL 0, monitor 3 startup-probe restarts, worker 0. Scheduler heartbeat was fresh at `2026-08-30T13:17:16Z`; no OOM flags are set. Public `/health/live` on the supplied 9444 route returned 404 due to the configured proxy route split, while private `/health/ready` and `/version` returned HTTP 200. Nginx is active with zero restarts and ingress works.
- PostgreSQL accepts connections at 8/100; migration revision is `0014_hunt_playbooks`, matching the local migration head. Counts: 1,895 source documents, 91 raw artifacts, 10,551 entity-evidence rows, 3,522 indicators, 178 reports, 193 report versions, 193 publications. Orphan evidence and orphan current-report references are both 0. No pending/failed migration or integrity evidence was found.
- Host capacity is healthy: root 38% used with 304 GiB available, 58 GiB memory available, load previously low, and file-nr 3,840 allocated against system limit 9.22e18; container memory is low. Nginx unprivileged `nginx -t` still fails because `/etc/nginx/ssl/tailscale/hermes.key` is root-only; no reload was attempted. TLS verification succeeds for `matrix-1.taild27e3c.ts.net`, Let's Encrypt YE1, valid through `2026-11-16T14:30:36Z`. OCSP stapling was not available to curl, but chain/hostname validation succeeded.
- Latest encrypted backup remains `/backups/hermes-20260829T203010Z.dump.enc`, mode 600, 3,054,096 bytes, completed `2026-08-29T20:30:10Z`; metadata SHA-256 matches the recomputed digest `c7d7bbdcd269479e459bc659dc2015f94e7a36d77fc084f5348ecb28ae23f00d`. No restore rehearsal was run and no backup was altered.
- Release/config identity is unchanged: repository `git@github.com:mrcoggsworth/threat-intel-agent.git`, branch `main`, HEAD `fa78a28426a6363be53f7fda225035d5dee171c3`, application `0.1.0`, mutable image `cti-hermes:local` with ID/digest `sha256:25b6b511409604ddda8195b870c8d9d0cae46feb0a677acfcddd6104f423721d`. Containers started around 2026-08-29 20:30Z; production env mtime is 2026-08-25 17:20:29 -0500. Checked-in Compose validation is blocked because the env lacks required `HERMES_SECRET_DIR` (and cannot independently satisfy immutable `HERMES_IMAGE`); no deployment record or approved compatible rollback target was found.
- Actions: read-only diagnosis and this local incident-record update only. No restart, migration, deployment, rollback, restore, publication mutation, source edit, credential change, Docker prune, volume deletion, or destructive recovery was performed. External issue creation remains blocked because `gh auth status` reports no authenticated host. Required approvals: reviewed source-registry repair, proxy/observability/configuration repair, any deployment or migration, and any rollback/restore.

## Current verification update — 2026-08-30 08:33 CDT (13:33 UTC)
- Incident remains active as degraded/partial-failure ingestion, not an availability outage. Preflight at 13:31–13:33 UTC found 7 failed ingestion runs and 0 completed runs. Latest run `05a4b4f8-33fc-568e-8fc8-77dbd2e770d4` remains failed (02:00:00.103778Z–02:00:01.918747Z), 14 total / 12 successful / 2 failed / 85 new documents; no fully successful run exists.
- Exact configured feed failures remain reproducible: Google TAG returns HTTP 404 `text/html` (227,758 bytes); MSRC redirects to `https://www.microsoft.com/en-us/msrc/blog` and returns HTTP 200 `text/html` (94,397 bytes), not RSS/XML. The other 12 source rows completed. Primary cause confidence remains high: stale/incompatible enabled upstream endpoints.
- Runtime is healthy: web, scheduler, backup, monitor, and PostgreSQL are running healthy; worker is intentionally exited 0 with the reserved-entrypoint behavior. Restart counts are web 0, scheduler 0, backup 0, PostgreSQL 0, monitor 3 (startup probe ordering), worker 0. Scheduler heartbeat file was fresh at 13:31:46Z; Docker events showed healthcheck/diagnostic execs only, with no restart/crash events.
- Correctly routed probes: supplied private `/health/ready` HTTP 200 (`configuration=ok`, `database=ok`), `/version` HTTP 200 (`0.1.0`), and public `/health/live` HTTP 200. Unauthenticated local operations paths returned 404; authenticated monitor requests in web logs returned 200. Nginx is active with zero restarts. TLS verification succeeds for `matrix-1.taild27e3c.ts.net`, Let's Encrypt YE1, valid through 2026-11-16 14:30:36Z. Unprivileged `nginx -t` remains blocked by the root-only private key; no reload attempted.
- PostgreSQL 16.14 accepts connections; 3 active sessions observed, 7 sessions with a wait event including health/diagnostic activity, Alembic `0014_hunt_playbooks` matches the local migration head, and no pending/failed migration evidence was found. Counts: 1,895 source documents, 91 raw artifacts, 10,551 entity-evidence rows, 3,522 indicators, 178 reports, 193 report versions, 193 publications; orphan non-null entity-evidence references and orphan current-report-version references are both 0.
- Latest encrypted backup is present and readable through the backup container: `/backups/hermes-20260829T203010Z.dump.enc`, mode 600, 3,054,096 bytes, completed 2026-08-29T20:30:10Z; computed SHA-256 matches metadata (`c7d7bbdcd269479e459bc659dc2015f94e7a36d77fc084f5348ecb28ae23f00d`). No restore rehearsal or backup mutation was performed.
- Host capacity is healthy: root filesystem 38% used with 304 GiB available, 55 GiB memory available, load 0.08/0.12/0.12, FD limit 4096, `/proc/sys/fs/file-nr` 3,840 allocated, and no observed OOM signal. Kernel OOM verification via `dmesg` was unavailable to the diagnostic user.
- Release/config identity is unchanged: repository `git@github.com:mrcoggsworth/threat-intel-agent.git`, branch `main`, HEAD `fa78a28426a6363be53f7fda225035d5dee171c3`, application `0.1.0`, mutable `cti-hermes:local`, image ID `sha256:25b6b511409604ddda8195b870c8d9d0cae46feb0a677acfcddd6104f423721d`; containers were created around 20:30Z on 2026-08-29. Production env mtime is 2026-08-25 17:20:29 -0500. Checked-in Compose validation fails when using that env because `HERMES_SECRET_DIR` is missing, but succeeds when the known protected secret directory is supplied; no deployment-state record exists and no approved compatible rollback target was found.
- Actions: diagnosis-only actions: no restart, migration, deployment, rollback, restore, source edit, credential change, Docker prune, volume deletion, publication mutation, or proxy reload. External issue creation remains blocked because `gh auth status` reports no authenticated GitHub host; this local record remains the durable maintenance handoff. Required approvals remain reviewed source-registry repair, proxy/observability/configuration repair, any deployment or migration, and any rollback/restore.

## Current verification update — 2026-08-30 14:17 UTC
- Incident remains active as degraded/partial-failure ingestion, not an availability outage. PostgreSQL records 7 `failed` ingestion runs and 0 `completed` runs. Latest run `05a4b4f8-33fc-568e-8fc8-77dbd2e770d4` remains `failed` (`02:00:00.103778Z`–`02:00:01.918747Z`), 14 total / 12 successful / 2 failed / 85 new documents; no later run had started at capture.
- Exact configured upstream probes remain reproducible: Google TAG returns HTTP/2 404 `text/html` (227,758 bytes); MSRC returns HTTP/2 301 to `https://www.microsoft.com/en-us/msrc/blog`, then HTTP/2 200 `text/html` (94,397 bytes), not RSS/XML. Latest source-run rows classify these as `http_error`/HTTP 404 and `malformed_xml`; all other 12 sources completed. Primary cause confidence remains high: stale/incompatible enabled upstream endpoints.
- Runtime is stable: web, scheduler, monitor, backup, and PostgreSQL are running and healthy; worker is intentionally exited 0 with its reserved-entrypoint behavior. Restart counts are web 0, scheduler 0, backup 0, PostgreSQL 0, monitor 3 startup-probe restarts, worker 0. Scheduler heartbeat was fresh at `2026-08-30T14:17:17Z`; Docker events in the observation window showed healthcheck/diagnostic execs only, with no crash or restart events.
- Supplied TLS endpoint probes returned private `/health/ready` 200 (`configuration=ok`, `database=ok`), `/version` 200 (`0.1.0`), and public `/health/live` on 9443 200. Nginx is active with zero restarts and ingress works. Unprivileged `nginx -t` still fails because `/etc/nginx/ssl/tailscale/hermes.key` is root-only; `sudo -n nginx -t` is unavailable because sudo requires a password. Certificate verification succeeds for `matrix-1.taild27e3c.ts.net`, Let's Encrypt YE1, valid through `2026-11-16T14:30:36Z`.
- PostgreSQL 16.14 accepts connections (`8/100`), has 0 waiting active locks, and is at Alembic `0014_hunt_playbooks`, matching the local migration head. Counts: 1,895 source documents, 91 raw artifacts, 10,551 entity-evidence rows, 3,522 indicators, 178 reports, 193 report versions, 193 publications, and 0 operational events. Orphan non-NULL entity-evidence references and orphan current-report-version references are both 0. No pending/failed migration or data-corruption evidence was found.
- Host capacity is healthy: root filesystem 38% used with 304 GiB available, 55 GiB memory available, load is low, system FD limit is 4096 with 3,776 allocated files, and no container OOM signal was observed. Latest encrypted backup `/backups/hermes-20260829T203010Z.dump.enc` is mode 600, 3,054,096 bytes; recomputed SHA-256 `c7d7bbdcd269479e459bc659dc2015f94e7a36d77fc084f5348ecb28ae23f00d` matches metadata. Backup completed `2026-08-29T20:30:10Z`; no restore rehearsal was run.
- Release/config identity is unchanged: repository `git@github.com:mrcoggsworth/threat-intel-agent.git`, branch `main`, HEAD `fa78a28426a6363be53f7fda225035d5dee171c3`, application `0.1.0`, mutable `cti-hermes:local`, image ID `sha256:25b6b511409604ddda8195b870c8d9d0cae46feb0a677acfcddd6104f423721d`. Containers were created around `2026-08-29T20:30Z`; production env mtime remains `2026-08-25 17:20:29 -0500`; checked-in Compose validation fails without required `HERMES_SECRET_DIR`/`HERMES_IMAGE` but succeeds when those known runtime values are supplied. No deployment-state record or approved compatible rollback target was found.
- Actions: read-only diagnosis and this local incident-record update only. No restart, migration, deployment, rollback, restore, publication mutation, source edit, credential change, Docker prune, volume deletion, or proxy reload was performed. External issue creation remains blocked because `gh auth status` reports no authenticated GitHub host. Required approvals remain reviewed source-registry repair, proxy/observability/configuration repair, any deployment or migration, and any rollback/restore.

## Current verification update — 2026-08-30 10:03 CDT (15:03 UTC)
- Incident remains active as degraded/partial-failure ingestion, not an availability outage. Current database state is 7 `failed` ingestion runs and 0 `completed` runs. Latest run `05a4b4f8-33fc-568e-8fc8-77dbd2e770d4` remains failed (`2026-08-30 02:00:00.103778Z`–`02:00:01.918747Z`), 14 total / 12 successful / 2 failed / 85 new documents; no later run exists.
- Exact configured upstream probes at 15:02 UTC reproduce the same failures: Google TAG returns HTTP/2 404 `text/html`, 227,758 bytes; MSRC returns HTTP/2 301 to `https://www.microsoft.com/en-us/msrc/blog`, followed by HTTP/2 200 `text/html`, 94,397 bytes, not RSS/XML. Schema-correct latest `source_run` rows classify them as `http_error`/HTTP 404 and `malformed_xml`; all other 12 sources completed. Primary cause confidence remains high: stale/incompatible enabled source endpoints.
- Runtime and restart state are stable: web, scheduler, monitor, backup, and PostgreSQL are running and healthy; worker is intentionally exited 0 with its reserved-entrypoint behavior. Restart counts are web 0, scheduler 0, backup 0, PostgreSQL 0, monitor 3 startup-probe restarts, worker 0; all application containers report `OOMKilled=false`. Fresh authenticated local ops probes returned metrics/readiness/version/last-success/heartbeat HTTP 200; readiness reports configuration/database `ok`, and heartbeat was `2026-08-30T15:03:17Z`. Public `/health/live` on 9443 and private `/health/ready` on 9444 both returned 200.
- PostgreSQL 16.14 accepts connections; 8 sessions of 100, 0 ungranted locks, Alembic `0014_hunt_playbooks` matches the local migration head, and no pending/failed migration evidence was found. Counts are 1,895 source documents and 91 raw artifacts; orphan non-NULL entity-evidence references and orphan current-report-version references are both 0. No database corruption or publication-integrity issue is indicated.
- Host capacity is healthy: root filesystem 38% used with 304 GiB available, 58 GiB available memory, load `0.32 0.21 0.17`, FD limit 4096 with 3,840 allocated files, and no observed container OOM signal. Nginx is active with zero restarts and ingress works. Unprivileged `nginx -t` fails because `/etc/nginx/ssl/tailscale/hermes.key` is root-only; no reload was attempted. TLS verification succeeds for `matrix-1.taild27e3c.ts.net`, Let's Encrypt YE1, valid through `2026-11-16T14:30:36Z`.
- Latest encrypted backup `/backups/hermes-20260829T203010Z.dump.enc` remains mode 600, 3,054,096 bytes, completed `2026-08-29T20:30:10Z`; recomputed SHA-256 `c7d7bbdcd269479e459bc659dc2015f94e7a36d77fc084f5348ecb28ae23f00d` matches metadata. No restore rehearsal or backup mutation was performed.
- Release/config identity is unchanged: repository `git@github.com:mrcoggsworth/threat-intel-agent.git`, branch `main`, HEAD `fa78a28426a6363be53f7fda225035d5dee171c3`, application `0.1.0`, mutable `cti-hermes:local` image ID/digest `sha256:25b6b511409604ddda8195b870c8d9d0cae46feb0a677acfcddd6104f423721d`. Containers started around 2026-08-29 20:30 UTC; `/opt/cti-hermes/env/production.env` mtime remains `2026-08-25 17:20:29 -0500`. Production Compose validation still exits 1 because that env omits required `HERMES_SECRET_DIR`; no deployment record or approved compatible rollback target was found.
- No recovery action was justified or performed: the defect is deterministic, data integrity is not in question, and service runtime is healthy. `gh auth status` remains unauthenticated, so no external issue was created; this repository-local incident record is the durable maintenance handoff. Required approvals remain reviewed source-registry repair, observability/proxy/configuration repair, any deployment or migration, and any rollback/restore.

## Current verification update — 2026-08-30 10:31 CDT (15:31 UTC)
- Incident remains active as degraded/partial-failure ingestion, not an availability outage. Live database checks still show 7 `failed` ingestion runs and 0 `completed` runs. Latest run `05a4b4f8-33fc-568e-8fc8-77dbd2e770d4` remains failed (`02:00:00.103778Z`–`02:00:01.918747Z`), 14 total / 12 successful / 2 failed / 85 new documents; no later run is present.
- Exact configured upstream probes reproduce the same primary failures: Google TAG returns HTTP 404 `text/html` (227,758 bytes); MSRC follows HTTP 301 to `https://www.microsoft.com/en-us/msrc/blog` and returns HTTP 200 `text/html` (94,397 bytes), not RSS/XML. The other 12 source rows completed. Cause confidence remains high: stale/incompatible enabled upstream endpoints.
- Runtime is healthy and stable: web, scheduler, monitor, backup, and PostgreSQL are up/healthy; worker exited 0 with its intentional reserved-entrypoint message. Restart counts are web 0, scheduler 0, backup 0, PostgreSQL 0, monitor 3 (startup-probe ordering), worker 0; all application containers have `OOMKilled=false`. Local probes for liveness, readiness, and version and correctly routed external probes returned HTTP 200. Scheduler logs show source collection failures; web logs show successful authenticated monitoring probes.
- PostgreSQL 16.14 accepts connections; 8 sessions, 0 active waiters, Alembic `0014_hunt_playbooks` matches the local head. Counts: 1,895 source documents, 91 raw artifacts, 10,551 entity-evidence rows, 3,522 indicators, 178 reports, 193 report versions, 193 publications. Non-null orphan entity-evidence references and orphan current-report-version references are both 0. Recent database errors are from malformed read-only diagnostic SQL or invalid roles, not application migration/write failures.
- Host capacity is healthy: root filesystem 38% used with 304 GiB available, 55 GiB available memory, load 0.19/0.18/0.17, FD limit 4096 with 3,808 allocated files; kernel `dmesg` was unavailable to the diagnostic user, with no container OOM signal. Nginx is active with zero restarts and ingress works. Unprivileged `nginx -t` fails because `/etc/nginx/ssl/tailscale/hermes.key` is root-only; `sudo -n nginx -t` requires a password, so no reload was attempted.
- Latest encrypted backup remains `/backups/hermes-20260829T203010Z.dump.enc`, mode 600, 3,054,096 bytes, metadata present and artifact SHA-256 previously verified against metadata; completed `2026-08-29T20:30:10Z`. No restore rehearsal or backup mutation was performed. TLS verification succeeds for `matrix-1.taild27e3c.ts.net`, Let's Encrypt YE1, valid through `2026-11-16T14:30:36Z`.
- Release/config identity is unchanged: repository `git@github.com:mrcoggsworth/threat-intel-agent.git`, branch `main`, HEAD `fa78a28426a6363be53f7fda225035d5dee171c3`, application `0.1.0`, mutable image `cti-hermes:local`, image ID/digest `sha256:25b6b511409604ddda8195b870c8d9d0cae46feb0a677acfcddd6104f423721d`. Containers were created around 20:30Z on 2026-08-29; production env mtime remains 2026-08-25 17:20:29 -0500. Compose validation using the configured `/opt/cti-hermes/env/production.env` could not run because the checked-in Compose path is not present at `/opt/cti-hermes/deploy/docker-compose.yml`; the live Compose label identifies the repository checkout path. No deployment record or approved compatible rollback target was found.
- Diagnosis-only action: no restart, migration, deployment, rollback, restore, credential change, Docker prune, volume deletion, proxy reload, or publication mutation was performed. `gh auth status` remains unauthenticated, so no external issue was created; this local incident record is the durable maintenance handoff. Required approvals remain reviewed source-registry repair, proxy/observability/configuration repair, deployment/migration, and rollback/restore.

## Current verification update — 2026-08-30 18:04 UTC
- Incident remains active as degraded/partial-failure ingestion, not an availability outage. Authenticated analyst status reports database `ok`, latest persisted run `05a4b4f8-33fc-568e-8fc8-77dbd2e770d4` at `2026-08-30T02:00:01.918747Z`, and no newer run; the run is `failed`, 14 total / 12 successful / 2 failed / 85 new documents. Scheduler heartbeat is fresh at `2026-08-30T18:03:48Z`.
- Exact configured probes still reproduce Google TAG HTTP 404 `text/html` and MSRC redirect to `https://www.microsoft.com/en-us/msrc/blog` followed by HTTP 200 `text/html`, not RSS/XML. The other 12 enabled sources succeed in the latest persisted run. Primary cause confidence remains high: stale/incompatible enabled upstream endpoints.
- Web, scheduler, monitor, backup, and PostgreSQL are running and healthy; worker exited 0 by design. Restart counts remain web 0, scheduler 0, backup 0, PostgreSQL 0, monitor 3 historical startup restarts, worker 0; application containers report `OOMKilled=false`. No lifecycle restart/crash events were observed in the last 2 hours. Local health/version probes and authenticated private operations probes returned 200.
- PostgreSQL 16.14 accepts connections, with 8 sessions and 0 ungranted locks; Alembic remains `0014_hunt_playbooks`, matching local head. No pending/failed migration or integrity evidence was found. No data mutation was performed.
- Host capacity remains healthy: 38% disk used/304 GiB available, 55 GiB memory available, FD limit 4096, and no observed container OOM signal. Nginx remains active with working ingress; unprivileged `nginx -t` is blocked by the root-only private key and passwordless sudo is unavailable. TLS validates through `2026-11-16T14:30:36Z`.
- Latest encrypted backup remains `/backups/hermes-20260829T203010Z.dump.enc`, mode 600, 3,054,096 bytes, completed `2026-08-29T20:30:10Z`, with SHA-256 matching metadata (`c7d7bbdcd269479e459bc659dc2015f94e7a36d77fc084f5348ecb28ae23f00d`). No restore rehearsal was run; backup freshness is approaching the 48-hour threshold.
- Release/config identity is unchanged: HEAD `fa78a28426a6363be53f7fda225035d5dee171c3`, app `0.1.0`, mutable `cti-hermes:local` image digest `sha256:25b6b511409604ddda8195b870c8d9d0cae46feb0a677acfcddd6104f423721d`; containers started around `2026-08-29T20:30Z`. Compose validates only with protected runtime interpolation because production env omits `HERMES_SECRET_DIR`; no approved immutable deployment record or compatible rollback target exists.
- Action: read-only diagnosis plus this local incident-record update. No restart, migration, deployment, rollback, restore, source edit, credential change, proxy reload, Docker prune, volume deletion, or publication mutation was performed. External GitHub issue creation remains blocked by unauthenticated `gh`; required approvals remain reviewed source repair, observability/proxy/configuration repair, deployment/migration, and rollback/restore.

## Current verification update — 2026-08-30 18:18 UTC
- Incident remains active as degraded/partial-failure ingestion, not an availability outage. Current PostgreSQL state is 7 failed ingestion runs, 0 completed runs, and 0 running runs. Latest run `05a4b4f8-33fc-568e-8fc8-77dbd2e770d4` remains failed (`2026-08-30 02:00:00.103778Z`–`02:00:01.918747Z`), with 14 total / 12 successful / 2 failed / 85 new documents. Authenticated `/api/v1/ops/last-success` reports `2026-08-30T02:00:01.918747+00:00` under the service's partial-success semantics; no fully completed run exists.
- Exact configured upstream probes at 18:17 UTC reproduce the same two failures: Google TAG returns HTTP/2 404 `text/html` (227,758 bytes); MSRC returns HTTP/2 301 to `https://www.microsoft.com/en-us/msrc/blog`, then HTTP/2 200 `text/html` (94,397 bytes), not RSS/XML. The latest `source_run` rows classify these as `http_error`/HTTP 404 and `malformed_xml`; all other 12 sources completed. Primary cause confidence remains high: stale/incompatible enabled upstream endpoints.
- Runtime is stable: web, scheduler, monitor, backup, and PostgreSQL are running and healthy; worker exited 0 by intentional reserved-entrypoint behavior (`restart: no`). Restart counts are web 0, scheduler 0, backup 0, PostgreSQL 0, monitor 3 historical startup-probe restarts, worker 0; all six CTI containers report `OOMKilled=false`. Docker events over the last six hours show healthcheck/diagnostic execs only, with no lifecycle restart or crash events. Scheduler heartbeat is fresh at `2026-08-30T18:18:18Z`.
- Correctly routed external probes returned public `/health/live` 200 on 9443, private `/health/ready` 200 on 9444 with `configuration=ok,database=ok`, and private `/version` 200 (`0.1.0`). The complementary split-port 404s are expected. Nginx is active with zero restarts and ingress works. TLS verification succeeds for `matrix-1.taild27e3c.ts.net`, Let's Encrypt YE1, valid 2026-08-18 through 2026-11-16 14:30:36Z. Unprivileged `nginx -t` remains blocked by root-only `/etc/nginx/ssl/tailscale/hermes.key`; passwordless sudo is unavailable, so no reload was attempted.
- PostgreSQL 16.14 accepts connections; 8 sessions observed, 7 with a wait event including health/diagnostic activity; Alembic `0014_hunt_playbooks` matches the local migration head. Counts: 1,895 source documents, 91 raw artifacts, 10,551 entity-evidence rows, 3,522 indicators, 1,784 vulnerabilities, 178 reports, 193 report versions, and 193 publications. Non-null orphan entity-evidence references and orphan current-report-version references are both 0. No pending/failed migration or data-corruption evidence was found; recent PostgreSQL errors are malformed diagnostic SQL probes, not application failures.
- Host capacity remains healthy: root filesystem 38% used with 304 GiB available, 55 GiB memory available, load 0.11/0.12/0.15 at capture, system FD limit 4096 with 3,776 allocated files, and no observed container OOM signal. Latest encrypted backup `/backups/hermes-20260829T203010Z.dump.enc` is mode 600, 3,054,096 bytes, completed `2026-08-29T20:30:10Z`; current SHA-256 matches metadata (`c7d7bbdcd269479e459bc659dc2015f94e7a36d77fc084f5348ecb28ae23f00d`) and `latest.metadata` is present. No restore rehearsal or backup mutation was performed.
- Release/config identity is unchanged: repository `git@github.com:mrcoggsworth/threat-intel-agent.git`, branch `main`, HEAD `fa78a28426a6363be53f7fda225035d5dee171c3`, application `0.1.0`, mutable `cti-hermes:local` image ID/digest `sha256:25b6b511409604ddda8195b870c8d9d0cae46feb0a677acfcddd6104f423721d`. Containers were created/started around `2026-08-29T20:30Z`; production env mtime remains `2026-08-25 17:20:29 -0500`; Compose validates when protected runtime interpolation is supplied, but no deployment-state record or approved compatible rollback target exists. `gh auth status` remains unauthenticated.
- Action: read-only diagnosis and this incident-record update only. No restart, migration, deployment, rollback, restore, source edit, credential change, proxy reload, Docker prune, volume deletion, or publication mutation was performed. Required approvals remain reviewed source-registry repair, observability/proxy/configuration repair, any deployment or migration, and any rollback/restore.

## Current verification update — 2026-08-30 18:50 UTC
- Incident remains active as degraded/partial-failure ingestion, not an availability outage. The latest database run remains `05a4b4f8-33fc-568e-8fc8-77dbd2e770d4` (`failed`, 14 total / 12 successful / 2 failed / 85 new documents); the database still has 7 failed runs and 0 completed runs.
- Exact upstream reproduction remains unchanged: Google TAG returns HTTP/2 404 `text/html` (227,758 bytes); MSRC returns HTTP/2 301 to `https://www.microsoft.com/en-us/msrc/blog`, then HTTP/2 200 `text/html` (94,397 bytes), not RSS/XML. The other 12 latest source rows are completed. Primary cause confidence remains high: stale/incompatible enabled source endpoints.
- Runtime preflight is healthy: web, scheduler, monitor, backup, and PostgreSQL are running/healthy; worker is intentionally exited 0 with its reserved-entrypoint behavior. Restart counts are web 0, scheduler 0, backup 0, PostgreSQL 0, monitor 3 historical startup-probe restarts, worker 0; all report `OOMKilled=false`. No lifecycle restart/crash/oom events were returned for the last 24 hours. Scheduler heartbeat was `2026-08-30T18:49:19Z` (mode 644, 21 bytes).
- Correctly routed TLS probes returned public `/health/live` 200, private `/health/ready` 200 (`configuration=ok`, `database=ok`), and `/version` 200 (`0.1.0`). Nginx is active with zero restarts and ingress works. TLS validates for `matrix-1.taild27e3c.ts.net`, Let's Encrypt YE1, through `2026-11-16T14:30:36Z`. Unprivileged `nginx -t` remains blocked by the root-only private key; passwordless sudo is unavailable, so no reload was attempted.
- PostgreSQL 16.14 accepts connections: 8/100 sessions, 0 ungranted locks, Alembic `0014_hunt_playbooks` matches the local head, and no pending/failed migration evidence was found. Counts remain 1,895 source documents, 91 raw artifacts, 10,551 entity-evidence rows, 178 reports, 193 report versions, and 193 publications; orphan non-NULL evidence references and orphan current-report-version references are both 0.
- Latest encrypted backup remains `/backups/hermes-20260829T203010Z.dump.enc`, mode 600, 3,054,096 bytes. Recomputed SHA-256 `c7d7bbdcd269479e459bc659dc2015f94e7a36d77fc084f5348ecb28ae23f00d` matches the recorded metadata; completion was `2026-08-29T20:30:10Z`. No restore rehearsal or backup mutation was performed; freshness is approaching the 48-hour threshold.
- Host capacity remains healthy: root filesystem 38% used with 304 GiB available, 55 GiB available memory, low load, FD limit 4096, and no observed container OOM signal. Production Compose validation succeeds when the known protected runtime interpolation is supplied, but the deployed env still omits `HERMES_SECRET_DIR`; the running image remains mutable `cti-hermes:local` with ID/digest `sha256:25b6b511409604ddda8195b870c8d9d0cae46feb0a677acfcddd6104f423721d`. No deployment record or approved compatible rollback target exists.
- Action: read-only diagnosis plus this incident-record update only. No restart, migration, deployment, rollback, restore, source edit, credential change, proxy reload, Docker prune, volume deletion, or publication mutation was performed. External GitHub issue creation remains blocked by unauthenticated `gh`; this repository-local incident record remains the durable maintenance handoff. Required approvals remain reviewed source repair, observability/proxy/configuration repair, deployment/migration, and rollback/restore.

## Current verification update — 2026-08-30 20:04 UTC
- Incident remains active as degraded/partial-failure ingestion, not an availability outage. Live PostgreSQL checks still show 7 failed ingestion runs and 0 completed runs; no running run is present. Latest run `05a4b4f8-33fc-568e-8fc8-77dbd2e770d4` remains failed (`2026-08-30 02:00:00.103778Z`–`02:00:01.918747Z`), 14 total / 12 successful / 2 failed / 85 new documents. Its partial-success timestamp remains the service's `/last-success` result; no fully completed run exists.
- Exact configured upstream probes remain reproducible: Google TAG returns HTTP 404 `text/html` (227,758 bytes); MSRC redirects to `https://www.microsoft.com/en-us/msrc/blog` and returns HTTP 200 `text/html` (94,397 bytes), not RSS/XML. Latest `source_run` rows classify these as `http_error`/404 and `malformed_xml`; the other 12 enabled sources completed. Primary cause confidence remains high: stale/incompatible enabled feed endpoints.
- Runtime is stable: web, scheduler, monitor, backup, and PostgreSQL are running and healthy; worker is intentionally exited 0 with its reserved-entrypoint behavior and `restart: no`. Restart counts are web 0, scheduler 0, backup 0, PostgreSQL 0, monitor 3 historical startup-probe restarts, worker 0; all CTI application containers report `OOMKilled=false`. Scheduler heartbeat was fresh at `2026-08-30T20:03:19Z`; no recovery restart was justified.
- Correctly routed probes returned private `/health/ready` HTTP 200 (`configuration=ok`, `database=ok`), `/version` HTTP 200 (`0.1.0`), and public `/health/live` HTTP 200. Nginx is active with zero restarts and ingress works. TLS verifies for `matrix-1.taild27e3c.ts.net`, Let's Encrypt YE1, through `2026-11-16T14:30:36Z`. Unprivileged `nginx -t` still fails on root-only `/etc/nginx/ssl/tailscale/hermes.key`; passwordless sudo is unavailable, and no reload was attempted.
- PostgreSQL 16.14 accepts connections; revision `0014_hunt_playbooks` matches local Alembic head, with no pending/failed migration evidence. Connection/capacity and integrity checks remain healthy: 8/100 sessions, no ungranted locks, and 0 orphan non-null entity-evidence references / 0 orphan current-report-version references. No database or publication-integrity failure is indicated.
- Host capacity remains healthy: root filesystem 38% used with 304 GiB available, 55 GiB memory available, load `0.25 0.17 0.18`, FD limit 4096 with 3,808 allocated files, low CTI-container memory, and no container OOM signal. Latest encrypted backup `/backups/hermes-20260829T203010Z.dump.enc` remains mode 600, 3,054,096 bytes; metadata and recomputed SHA-256 `c7d7bbdcd269479e459bc659dc2015f94e7a36d77fc084f5348ecb28ae23f00d` match, completed `2026-08-29T20:30:10Z`, within the 48-hour freshness threshold. No restore rehearsal or backup mutation was performed.
- Release/config identity is unchanged: repository HEAD `fa78a28426a6363be53f7fda225035d5dee171c3`, application `0.1.0`, mutable `cti-hermes:local` image ID/digest `sha256:25b6b511409604ddda8195b870c8d9d0cae46feb0a677acfcddd6104f423721d`; containers started around `2026-08-29T20:30Z`. Compose labels identify the checked-out compose path and `/opt/cti-hermes/env/production.env` (mtime `2026-08-25 17:20:29 -0500`); checked-in Compose validation cannot run without protected runtime interpolation (`HERMES_SECRET_DIR`/`HERMES_IMAGE`). No deployment record or approved compatible rollback target exists.
- Action: diagnosis-only verification and this incident-record update. No restart, migration, deployment, rollback, restore, source edit, credential change, proxy reload, Docker prune, volume deletion, or publication mutation was performed. `gh auth status` remains unauthenticated, so no external issue was created; this local incident record is the durable maintenance handoff. Required approvals remain reviewed source repair, observability/proxy/configuration repair, any deployment/migration, and any rollback/restore.

## Current verification update — 2026-08-30 20:48 UTC
- Incident remains active as degraded/partial-failure ingestion, not an availability outage. Live PostgreSQL state is unchanged: 7 failed ingestion runs, 0 completed runs, 0 running runs; latest run `05a4b4f8-33fc-568e-8fc8-77dbd2e770d4` remains failed (02:00:00.103778Z–02:00:01.918747Z), 14 total / 12 successful / 2 failed / 85 new documents. No later successful run exists.
- The two configured upstream failures remain reproducible at 20:48 UTC: Google TAG returns HTTP/2 404 `text/html` (227,758 bytes); MSRC returns HTTP/2 301 to `https://www.microsoft.com/en-us/msrc/blog`, then HTTP/2 200 `text/html` (94,397 bytes), not RSS/XML. The latest persisted source rows classify these as `http_error` and `malformed_xml`; the other 12 sources completed. Cause confidence remains high: stale/incompatible enabled feed endpoints.
- Runtime is healthy and stable: web, scheduler, monitor, backup, and PostgreSQL are running/healthy; worker exited 0 with its intentional reserved-entrypoint behavior (`restart: no`). Restart counts are web 0, scheduler 0, backup 0, PostgreSQL 0, monitor 3 historical startup-probe restarts, worker 0; all application containers report `OOMKilled=false`. Scheduler heartbeat was fresh at 20:47:20Z. Recent scheduler logs contain the two source collection failures; no application crash was observed.
- Correctly routed external probes returned public `/health/live` 200 on 9443, private `/health/ready` 200 on 9444 with `configuration=ok,database=ok`, and `/version` 200 with application `0.1.0`. Local liveness/readiness/version also returned 200. Nginx is active with zero restarts and ingress works. TLS validates with hostname/chain verification (`Let's Encrypt YE1`) through `2026-11-16T14:30:36Z`; unprivileged `nginx -t` remains blocked by the root-only private key and no reload was attempted.
- PostgreSQL 16.14 accepts connections; 8/100 sessions observed and 0 active waiting sessions in the direct check. Alembic revision is `0014_hunt_playbooks`, matching local `uv run alembic heads`; no pending/failed migration evidence. Counts are 1,895 source documents, 91 raw artifacts, 10,551 entity-evidence rows, 3,522 indicators, 178 reports, 193 report versions, and 193 publications; orphan evidence/current-report references are both 0. Recent PostgreSQL errors are malformed read-only diagnostic SQL or invalid-role probes, not application write/migration failures.
- Host capacity is healthy: root filesystem 38% used with 304 GiB available, 55 GiB memory available, low load, FD limit 4096 with 3,808 allocated files, and no container OOM signal. Latest encrypted backup `/backups/hermes-20260830T203010Z.dump.enc` is mode 600, 3,108,480 bytes, completed 20:30:11Z; recomputed SHA-256 `84ba57e9729b788dab98f98f90875a1ecc7713c7b06aecf2c9d865ad2ee9cf6d` matches metadata and `latest.metadata` points to it. No restore rehearsal or backup mutation was performed.
- Release/config identity: repository `git@github.com:mrcoggsworth/threat-intel-agent.git`, branch `main`, HEAD `fa78a28426a6363be53f7fda225035d5dee171c3`, application `0.1.0`, mutable image `cti-hermes:local` with content digest `sha256:25b6b511409604ddda8195b870c8d9d0cae46feb0a677acfcddd6104f423721d`. Containers were created/started around 20:30Z on 2026-08-29. Production env mtime is `2026-08-25 17:20:29 -0500`; checked-in Compose validates only when protected runtime `HERMES_SECRET_DIR` and `HERMES_IMAGE` are supplied, indicating deployment/config drift. No deployment-state record or approved compatible rollback target exists.
- Action: diagnosis-only verification and this incident-record update. No restart, migration, deployment, rollback, restore, source edit, credential change, proxy reload, Docker prune, volume deletion, or publication mutation was performed. External GitHub issue creation remains blocked by unauthenticated `gh`; this repository-local incident record remains the durable maintenance handoff. Required approvals remain reviewed source repair, observability/proxy/configuration repair, any deployment or migration, and any rollback/restore.

## Current verification update — 2026-08-30 21:03 UTC
- Incident remains active as degraded/partial-failure ingestion, not an availability outage. Live PostgreSQL state is 7 failed ingestion runs, 0 completed runs, and 0 running runs. Latest run `05a4b4f8-33fc-568e-8fc8-77dbd2e770d4` remains `failed` (`02:00:00.103778Z`–`02:00:01.918747Z`), with 14 total / 12 successful / 2 failed / 85 new documents; no later run is present.
- Exact authoritative registry probes reproduce the same two failures: Google Threat Analysis Group `https://blog.google/threat-analysis-group/rss/` returns HTTP/2 404 `text/html` (227,758 bytes); Microsoft Security Response Center `https://msrc.microsoft.com/blog/feed` returns HTTP/2 301 to `https://www.microsoft.com/en-us/msrc/blog`, then HTTP/2 200 `text/html` (94,397 bytes), not RSS/XML. The other 12 enabled sources completed in the latest run. Primary cause confidence remains high: stale/incompatible enabled upstream feed endpoints. Failed source-run records are retained.
- Runtime is stable: web, scheduler, monitor, backup, and PostgreSQL are running and healthy; worker is intentionally exited 0 with its reserved-entrypoint behavior and `restart: no`. Restart counts are web 0, scheduler 0, backup 0, PostgreSQL 0, monitor 3 historical startup-probe restarts, worker 0; application containers report `OOMKilled=false`. Scheduler heartbeat was fresh at `2026-08-30T21:02:50Z`. Docker events in the observation window contained healthcheck/diagnostic execs only, with no lifecycle restart or crash events.
- Correctly routed TLS probes returned public `/health/live` HTTP 200 on 9443, private `/health/ready` HTTP 200 on 9444 (`configuration=ok`, `database=ok`), and `/version` HTTP 200 (`0.1.0`). Nginx is active with zero restarts and ingress works. TLS hostname/chain verification succeeds for `matrix-1.taild27e3c.ts.net`, issued by Let's Encrypt YE1, valid through `2026-11-16T14:30:36Z`. Unprivileged `nginx -t` fails because the root-only private key is unreadable; `sudo -n nginx -t` requires a password, so no reload was attempted.
- PostgreSQL 16.14 accepts connections (`3/100` at the direct probe), Alembic revision is `0014_hunt_playbooks` and matches `uv run alembic heads`; no pending/failed migration evidence was found. Integrity counts are 0 orphan non-null entity-evidence references and 0 orphan current-report-version references. Counts are 1,895 source documents, 91 raw artifacts, 10,551 entity-evidence rows, 3,522 indicators, 178 reports, 193 report versions, and 193 publications. No database corruption or publication-integrity failure is indicated.
- Host capacity is healthy: root filesystem 38% used with 304 GiB available, 55 GiB memory available, load 0.10/0.11/0.13 at capture, FD limit 4096 with 3,840 allocated system files, low container memory, and no observed OOM signal. Latest encrypted backup `/backups/hermes-20260830T203010Z.dump.enc` is mode 600, 3,108,480 bytes, completed `2026-08-30T20:30:11Z`; recomputed SHA-256 `84ba57e9729b788dab98f98f90875a1ecc7713c7b06aecf2c9d865ad2ee9cf6d` matches metadata and `latest.metadata`. No restore rehearsal or backup mutation was performed.
- Release/config identity is unchanged: repository `git@github.com:mrcoggsworth/threat-intel-agent.git`, branch `main`, HEAD `fa78a28426a6363be53f7fda225035d5dee171c3`, application `0.1.0`, mutable `cti-hermes:local` with image ID/digest `sha256:25b6b511409604ddda8195b870c8d9d0cae46feb0a677acfcddd6104f423721d`. Containers were started around `2026-08-29T20:30Z`; production env mtime is `2026-08-25 17:20:29 -0500`. Checked-in Compose validation fails with the deployed env because required `HERMES_SECRET_DIR` and `HERMES_IMAGE` are absent, but succeeds with protected runtime interpolation; this is deployment/config drift, not the ingestion cause. No deployment record or approved compatible rollback target exists.
- Action: read-only diagnosis and this local incident-record update only. No restart, migration, deployment, rollback, restore, source edit, credential change, proxy reload, Docker prune, volume deletion, or publication mutation was performed. External GitHub issue creation remains blocked by unauthenticated `gh`; this repository-local incident record remains the durable maintenance handoff. Required approvals remain reviewed source repair, observability/proxy/configuration repair, any deployment or migration, and any rollback/restore.

## Current verification update — 2026-08-30 21:32 UTC
- Incident remains active as degraded/partial-failure ingestion, not an availability outage. Authenticated private `/api/v1/ops/last-success` still reports `2026-08-30T02:00:01.918747+00:00`; scheduler heartbeat is current at `2026-08-30T21:32:20Z`. No later successful or partial-success run was observed.
- Exact authoritative feed probes still reproduce Google TAG HTTP 404 `text/html` and MSRC 301-to-HTML rather than RSS/XML. The other 12 enabled feeds are unaffected. Primary cause confidence remains high: stale/incompatible enabled feed endpoints.
- External probes are healthy: public `/health/live` on 9443 and private readiness/version/ops endpoints on 9444 all returned HTTP 200. Nginx is active with zero restarts; TLS validates through `2026-11-16T14:30:36Z`.
- Containers remain stable and healthy; worker exited 0 by design. Restart counts remain web 0, scheduler 0, backup 0, PostgreSQL 0, monitor 3; all inspected CTI containers report `OOMKilled=false`, with no lifecycle or OOM events. PostgreSQL accepts connections and Alembic `0014_hunt_playbooks` matches local head; no pending/failed migration or integrity issue was found.
- Capacity is healthy: 38% disk used/304 GiB available, 55 GiB memory available, low container memory, and 3,840 allocated files against the system limit. Backup `/backups/hermes-20260830T203010Z.dump.enc` is mode 600, 3,108,480 bytes, completed 20:30:11Z, and SHA-256 `84ba57e9729b788dab98f98f90875a1ecc7713c7b06aecf2c9d865ad2ee9cf6d` matches metadata.
- Release/config remains unchanged at HEAD `fa78a28426a6363be53f7fda225035d5dee171c3`, app `0.1.0`, mutable `cti-hermes:local` image digest `sha256:25b6b511409604ddda8195b870c8d9d0cae46feb0a677acfcddd6104f423721d`; Compose validation succeeds only when the protected `HERMES_SECRET_DIR` is supplied explicitly. No approved compatible rollback target exists.
- Action: diagnosis-only verification and this local incident-record update only. No restart, migration, deployment, rollback, restore, source edit, credential change, proxy reload, Docker prune, volume deletion, or publication mutation was performed. GitHub issue creation remains blocked by unauthenticated `gh`; required approvals remain reviewed source repair, observability/proxy/configuration repair, deployment/migration, and rollback/restore.

## Current verification update — 2026-08-30 21:48 UTC
- Incident remains active as degraded/partial-failure ingestion, not an availability outage. Live PostgreSQL reports 7 failed runs, 0 completed runs, and 0 running runs. Latest run `05a4b4f8-33fc-568e-8fc8-77dbd2e770d4` is failed (`02:00:00.103778Z`–`02:00:01.918747Z`), 14 total / 12 successful / 2 failed / 85 new documents. No later successful run exists.
- Exact authoritative feed probes still reproduce Google TAG HTTP 404 `text/html` and MSRC HTTP 301 to `https://www.microsoft.com/en-us/msrc/blog` followed by HTTP 200 HTML, not RSS/XML. The other 12 source rows completed. Primary cause confidence remains high: stale/incompatible enabled upstream feed endpoints; failed evidence is retained.
- Runtime is healthy: web, scheduler, monitor, backup, and PostgreSQL are running/healthy; worker exited 0 by its intentional reserved-entrypoint behavior (`restart: no`). Restart counts are web 0, scheduler 0, backup 0, PostgreSQL 0, monitor 3 historical startup-probe restarts, worker 0; all inspected CTI containers report `OOMKilled=false`. Heartbeat is fresh at `2026-08-30T21:47:50Z`; no lifecycle restart/crash events were observed in the diagnostic window.
- Correctly routed supplied TLS probes returned public `/health/live` 200 on 9443, private `/health/ready` 200 on 9444 with `configuration=ok,database=ok`, and `/version` 200 with `0.1.0`. Nginx is active with zero restarts and ingress works. Certificate hostname/chain verification succeeds for `matrix-1.taild27e3c.ts.net`, Let's Encrypt YE1, valid through `2026-11-16T14:30:36Z`; unprivileged `nginx -t` remains blocked by the root-only private key and passwordless sudo is unavailable, so no reload was attempted.
- PostgreSQL 16.14 accepts connections; Alembic `0014_hunt_playbooks` matches `uv run alembic heads`, with 1 active session of 100 and 0 active waiters. Integrity checks found 0 orphan non-null entity-evidence references and 0 orphan current-report-version references. Counts: 1,895 source documents, 91 raw artifacts, 10,551 entity-evidence rows, 3,522 indicators, 1,784 vulnerabilities, 178 reports, 193 report versions, 193 publications, 0 operational events. No pending/failed migration or corruption evidence was found.
- Host capacity is healthy: root filesystem 38% used with 304 GiB available, 55 GiB memory available, low load, and no container OOM signal. Latest encrypted backup `/backups/hermes-20260830T203010Z.dump.enc` is mode 600, 3,108,480 bytes, completed `20:30:11Z`; recomputed SHA-256 `84ba57e9729b788dab98f98f90875a1ecc7713c7b06aecf2c9d865ad2ee9cf6d` matches metadata. No restore rehearsal or backup mutation was performed.
- Release/config identity is unchanged: repository `git@github.com:mrcoggsworth/threat-intel-agent.git`, branch `main`, HEAD `fa78a28426a6363be53f7fda225035d5dee171c3`, application `0.1.0`, mutable `cti-hermes:local` image ID/digest `sha256:25b6b511409604ddda8195b870c8d9d0cae46feb0a677acfcddd6104f423721d`, containers started around `2026-08-29T20:30Z`. The deployed env mtime remains `2026-08-25 17:20:29 -0500`; Compose validates with protected runtime interpolation but the env omits required `HERMES_SECRET_DIR`/immutable `HERMES_IMAGE`, a deployment/config drift finding. No deployment record or approved compatible rollback target exists.
- Action: diagnosis-only verification and this local incident-record update. No restart, migration, deployment, rollback, restore, source edit, credential change, proxy reload, Docker prune, volume deletion, or publication mutation was performed. External GitHub issue creation remains blocked by unauthenticated `gh`; this local record is the durable maintenance handoff. Required approvals remain reviewed source repair, observability/proxy/configuration repair, deployment/migration, and rollback/restore.

## Current verification update — 2026-08-30 23:32 UTC
- Incident remains active as degraded/partial-failure ingestion, not an availability outage. Live PostgreSQL reports 7 failed ingestion runs and 0 completed runs. Latest run `05a4b4f8-33fc-568e-8fc8-77dbd2e770d4` remains failed (`2026-08-30T02:00:00.103778Z`–`02:00:01.918747Z`), 14 total / 12 successful / 2 failed / 85 new documents; no newer run is present at capture.
- Exact authoritative upstream probes reproduce the persistent source defects: Google TAG returns HTTP/2 404 `text/html` (227,758 bytes); MSRC returns HTTP/2 301 to `https://www.microsoft.com/en-us/msrc/blog`, then HTTP/2 200 `text/html` (94,397 bytes), not RSS/XML. Latest source rows classify these as `http_error`/404 and `malformed_xml`; the other 12 sources completed. Cause confidence remains high: stale/incompatible enabled source endpoints.
- Runtime is stable: web, scheduler, monitor, backup, and PostgreSQL are running healthy; worker exited 0 by its reserved-entrypoint behavior (`restart: no`). Restart counts are web 0, scheduler 0, backup 0, PostgreSQL 0, monitor 3 historical startup-probe restarts, worker 0; all CTI containers report `OOMKilled=false`. Docker events in the last 24 hours show healthcheck/diagnostic execs only, with no lifecycle restart or crash events. Scheduler heartbeat is fresh at capture.
- Correctly routed external probes returned public `/health/live` 200 on 9443, private `/health/ready` 200 on 9444 with `configuration=ok,database=ok`, and private `/version` 200 (`0.1.0`). Nginx is active with zero restarts and TLS validates for `matrix-1.taild27e3c.ts.net`, Let's Encrypt YE1, through `2026-11-16T14:30:36Z`. Unprivileged `nginx -t` remains blocked by the root-only private key; no reload was attempted.
- PostgreSQL 16.14 accepts connections; Alembic `0014_hunt_playbooks` matches the local head; 8/100 connections and 0 active waiters were observed. Counts are 1,895 source documents, 91 raw artifacts, 10,551 entity-evidence rows, 3,522 indicators, 178 reports, 193 report versions, and 193 publications. Orphan non-null entity-evidence references and orphan current-report-version references are both 0. No pending/failed migration or integrity evidence was found.
- Host capacity is healthy: root filesystem 38% used with 304 GiB available, 55 GiB memory available, load 0.26/0.15/0.14, FD limit 4096 with 3,808 allocated files, and no observed container OOM signal. Latest encrypted backup `/backups/hermes-20260830T203010Z.dump.enc` is mode 600, 3,108,480 bytes, completed `2026-08-30T20:30:11Z`; recomputed SHA-256 `84ba57e9729b788dab98f98f90875a1ecc7713c7b06aecf2c9d865ad2ee9cf6d` matches metadata and `latest.metadata`. No restore rehearsal was run.
- Release/config identity is unchanged: repository `git@github.com:mrcoggsworth/threat-intel-agent.git`, branch `main`, HEAD `fa78a28426a6363be53f7fda225035d5dee171c3`, application `0.1.0`, mutable `cti-hermes:local` image ID/digest `sha256:25b6b511409604ddda8195b870c8d9d0cae46feb0a677acfcddd6104f423721d`. Production env mtime remains `2026-08-25 17:20:29-05:00`; checked-in Compose validates only with protected runtime interpolation and the deployed env omits required `HERMES_SECRET_DIR`/immutable `HERMES_IMAGE`. No deployment record or approved compatible rollback target exists.
- Action: diagnosis-only verification and this incident-record update. No restart, migration, deployment, rollback, restore, source edit, credential change, proxy reload, Docker prune, volume deletion, or publication mutation was performed. `gh auth status` remains unauthenticated, so no external issue was created. Required approvals remain reviewed source repair, observability/proxy/configuration repair, deployment/migration, and rollback/restore.

## Current verification update — 2026-08-30 23:47 UTC
- Incident remains active as degraded/partial-failure ingestion, not an availability outage. Live PostgreSQL state is unchanged at 7 failed ingestion runs, 0 completed runs, and 0 running runs. Latest run `05a4b4f8-33fc-568e-8fc8-77dbd2e770d4` remains failed (`02:00:00.103778Z`–`02:00:01.918747Z`), 14 total / 12 successful / 2 failed / 85 new documents.
- Exact authoritative feed probes at 23:47 UTC reproduce the same failures: Google TAG returns HTTP/2 404 `text/html` (227,758 bytes); MSRC returns HTTP/2 301 to `https://www.microsoft.com/en-us/msrc/blog`, then HTTP/2 200 `text/html` (94,397 bytes), not RSS/XML. The other 12 source rows completed. Primary cause confidence remains high: stale/incompatible enabled upstream feed endpoints; failed evidence remains retained.
- Runtime is stable: web, scheduler, monitor, backup, and PostgreSQL are running/healthy; worker exited 0 by intentional reserved-entrypoint behavior. Restart counts are web 0, scheduler 0, backup 0, PostgreSQL 0, monitor 3 historical startup-probe restarts, worker 0; inspected application containers report `OOMKilled=false`. Docker events in the last 24 hours show healthcheck/diagnostic execs only, with no lifecycle restart/crash events. Scheduler heartbeat remains fresh.
- Correctly routed TLS probes returned public `/health/live` 200 on 9443, private `/health/ready` 200 on 9444 (`configuration=ok`, `database=ok`), and `/version` 200 (`0.1.0`). Nginx is active with zero restarts and ingress works. TLS verifies for `matrix-1.taild27e3c.ts.net`, Let's Encrypt YE1, through `2026-11-16T14:30:36Z`. Unprivileged `nginx -t` remains blocked by the root-only private key; no reload was attempted.
- PostgreSQL 16.14 accepts connections; Alembic `0014_hunt_playbooks` matches the local head; no pending/failed migration or integrity evidence was found. Existing checks show 0 orphan non-null entity-evidence references and 0 orphan current-report-version references. Host capacity remains healthy: 38% disk used/304 GiB available, 55 GiB available memory, FD limit 4096 with 3,808 allocated files, and no observed container OOM signal.
- Latest encrypted backup `/backups/hermes-20260830T203010Z.dump.enc` remains present, mode 600, 3,108,480 bytes, completed `2026-08-30T20:30:11Z`; metadata SHA-256 matches `84ba57e9729b788dab98f98f90875a1ecc7713c7b06aecf2c9d865ad2ee9cf6d`. No restore rehearsal or backup mutation was performed.
- Release/config identity is unchanged: repository `git@github.com:mrcoggsworth/threat-intel-agent.git`, branch `main`, HEAD `fa78a28426a6363be53f7fda225035d5dee171c3`, application `0.1.0`, mutable `cti-hermes:local` image ID/digest `sha256:25b6b511409604ddda8195b870c8d9d0cae46feb0a677acfcddd6104f423721d`. The deployed env remains mtime `2026-08-25 17:20:29-05:00` and omits required `HERMES_SECRET_DIR`/immutable `HERMES_IMAGE`; no deployment record or approved compatible rollback target exists.
- Action: read-only diagnosis plus this incident-record update. No restart, migration, deployment, rollback, restore, source edit, credential change, proxy reload, Docker prune, volume deletion, or publication mutation was performed. External issue creation remains blocked by unauthenticated `gh`; required approvals remain reviewed source repair, observability/proxy/configuration repair, deployment/migration, and rollback/restore.


## Current verification update — 2026-08-31 00:20 UTC
- Incident remains active as degraded/partial-failure ingestion, not an availability outage. Live PostgreSQL state remains 7 failed ingestion runs, 0 completed runs, and 0 running runs. Latest run `05a4b4f8-33fc-568e-8fc8-77dbd2e770d4` remains failed with 14 total / 12 successful / 2 failed / 85 new documents; no newer run has started before the next scheduled window.
- Exact authoritative probes at 00:17 UTC still reproduce Google TAG HTTP/2 404 `text/html` and MSRC HTTP/2 301-to-HTML rather than RSS/XML. The other 12 source rows completed. Primary cause confidence remains high: stale/incompatible enabled feed endpoints; failed evidence remains retained.
- Web, scheduler, monitor, backup, and PostgreSQL are running healthy; worker is intentionally exited 0. Restart counts remain web 0, scheduler 0, backup 0, PostgreSQL 0, monitor 3 historical, worker 0; all CTI containers report `OOMKilled=false`. Scheduler heartbeat was fresh at `2026-08-31T00:17:51Z`; no restart/crash action was taken.
- Public `/health/live` on 9443, private `/health/ready` on 9444, and `/version` returned 200. PostgreSQL 16.14 accepts connections; revision `0014_hunt_playbooks` matches local head; 3 active connections, 0 active waiters, and both orphan-reference checks are 0. Counts remain 1,895 source documents, 91 raw artifacts, 10,551 entity-evidence rows, 3,522 indicators, 1,784 vulnerabilities, 178 reports, 193 report versions, and 193 publications.
- Host capacity is healthy: 38% disk used/304 GiB available, 55 GiB memory available, load `0.33 0.21 0.18`, FD limit 4096 with 3,872 allocated files, and no observed container OOM signal. Backup `/backups/hermes-20260830T203010Z.dump.enc` is mode 600, 3,108,480 bytes, completed 20:30:11Z, with metadata present and the latest recorded SHA-256 match. TLS remains valid through `2026-11-16T14:30:36Z`; unprivileged Nginx validation remains blocked by the root-only key and passwordless sudo is unavailable.
- Release/configuration is unchanged at HEAD `fa78a28426a6363be53f7fda225035d5dee171c3`, app `0.1.0`, mutable image digest `sha256:25b6b511409604ddda8195b870c8d9d0cae46feb0a677acfcddd6104f423721d`. Compose validation still requires protected `HERMES_SECRET_DIR` and immutable `HERMES_IMAGE`; no deployment record or approved compatible rollback target exists.
- Action: diagnosis-only verification and this local incident-record update. No restart, migration, deployment, rollback, restore, source edit, credential change, proxy reload, Docker prune, volume deletion, or publication mutation was performed. External GitHub issue creation remains blocked by unauthenticated `gh`; required approvals remain reviewed source repair, observability/proxy/configuration repair, deployment/migration, and rollback/restore.

## Current verification update — 2026-08-31 00:33 UTC
- Incident remains active as degraded/partial-failure ingestion, not an availability outage. PostgreSQL reports 7 failed ingestion runs, 0 completed runs, and 0 running runs. Latest run `05a4b4f8-33fc-568e-8fc8-77dbd2e770d4` remains failed (`2026-08-30T02:00:00.103778Z`–`02:00:01.918747Z`), 14 total / 12 successful / 2 failed / 85 new documents; no newer run has started.
- The authoritative source-run rows still identify exactly two failures: Google Threat Analysis Group `http_error`/HTTP 404 and Microsoft Security Response Center `malformed_xml`/`XML payload could not be parsed`; the other 12 sources completed. Direct probes at 00:32 UTC reproduce Google HTTP/2 404 HTML and MSRC HTTP/2 301 followed by HTML, not RSS/XML. Primary cause confidence remains high: stale/incompatible enabled feed endpoints; failed evidence is retained.
- Runtime is stable: web, scheduler, monitor, backup, and PostgreSQL are running and healthy; worker is intentionally exited 0 with its reserved-entrypoint behavior and `restart: no`. Restart counts are web 0, scheduler 0, backup 0, PostgreSQL 0, monitor 3 historical startup-probe restarts, worker 0; inspected CTI containers report `OOMKilled=false`. Scheduler heartbeat returned `2026-08-31T00:32:51Z`. Authenticated local ops version, last-success, scheduler-heartbeat, and metrics all returned HTTP 200; supplied private `/health/ready` and `/version` returned HTTP 200, while public liveness is served on the separate 9443 route.
- PostgreSQL 16.14 accepts connections; Alembic revision is `0014_hunt_playbooks`, matching the repository head. Direct checks found 0 orphan non-null entity-evidence references and 0 orphan current-report-version references. Counts are 1,895 source documents, 91 raw artifacts, 10,551 entity-evidence rows, 3,522 indicators, 1,784 vulnerabilities, 178 reports, 193 report versions, and 193 publications. No pending/failed migration or data-corruption evidence was found; recent PostgreSQL errors are from malformed diagnostic SQL probes, not application writes.
- Host capacity is healthy: root filesystem 38% used with 304 GiB available, load 0.11/0.11/0.12, system FD limit 4,096 with 3,840 allocated files, and no observed container OOM signal. Nginx is active with zero restarts and ingress works. TLS hostname/chain verification succeeds for `matrix-1.taild27e3c.ts.net`, issued by Let's Encrypt YE1, through `2026-11-16T14:30:36Z`. Unprivileged `nginx -t` remains blocked by the root-only private key; no reload was attempted.
- Backup state is healthy: `/backups/hermes-20260830T203010Z.dump.enc`, mode 600, 3,108,480 bytes, completed `2026-08-30T20:30:11Z`; recomputed SHA-256 `84ba57e9729b788dab98f98f90875a1ecc7713c7b06aecf2c9d865ad2ee9cf6d` matches `latest.metadata`. No restore rehearsal or backup mutation was performed.
- Release/configuration identity is unchanged: repository `git@github.com:mrcoggsworth/threat-intel-agent.git`, branch `main`, HEAD `fa78a28426a6363be53f7fda225035d5dee171c3`, application `0.1.0`, mutable image `cti-hermes:local` with content digest `sha256:25b6b511409604ddda8195b870c8d9d0cae46feb0a677acfcddd6104f423721d`. Containers were created/started around `2026-08-29T20:30Z`; production env mtime remains `2026-08-25 17:20:29-05:00`. Compose validates with protected runtime interpolation, but the deployed env omits required `HERMES_SECRET_DIR`/immutable `HERMES_IMAGE`; no deployment record or approved compatible rollback target exists.
- Action: diagnosis-only verification plus this durable incident-record update. No restart was justified because runtime and data integrity are healthy; no migration, deployment, rollback, restore, source edit, credential change, proxy reload, Docker prune, volume deletion, or publication mutation was performed. `gh auth status` remains unauthenticated, so no external issue was created. Required approvals remain reviewed source repair, observability/proxy/configuration repair, deployment/migration, and rollback/restore.

## Current verification update — 2026-08-31 00:47 UTC
- Incident remains active as degraded/partial-failure ingestion, not an availability outage. Live PostgreSQL state is 7 failed ingestion runs, 0 completed runs, and 0 running runs. Latest run `05a4b4f8-33fc-568e-8fc8-77dbd2e770d4` remains failed (`2026-08-30T02:00:00.103778Z`–`02:00:01.918747Z`), 14 total / 12 successful / 2 failed / 85 new documents; no newer run exists.
- Exact authoritative upstream probes at 00:47 UTC reproduce the two failures: Google TAG returns HTTP/2 404 `text/html` (227,758 bytes); MSRC returns HTTP/2 301 to `https://www.microsoft.com/en-us/msrc/blog`, then HTTP/2 200 `text/html` (94,397 bytes), not RSS/XML. The other 12 source rows completed. Primary cause confidence remains high: stale/incompatible enabled feed endpoints; failed evidence remains retained.
- Runtime is stable: web, scheduler, monitor, backup, and PostgreSQL are running and healthy; worker is intentionally exited 0 with its reserved-entrypoint behavior and `restart: no`. Restart counts are web 0, scheduler 0, backup 0, PostgreSQL 0, monitor 3 historical startup-probe restarts, worker 0; all application containers report `OOMKilled=false`. Scheduler heartbeat and authenticated monitor probes are fresh/HTTP 200; Docker events in the observation window show healthcheck/diagnostic execs only.
- Correctly routed TLS probes returned public `/health/live` 200 on 9443, private `/health/ready` 200 on 9444 (`configuration=ok`, `database=ok`), and `/version` 200 (`0.1.0`). Nginx is active with zero restarts and ingress works. TLS verifies for `matrix-1.taild27e3c.ts.net`, Let's Encrypt YE1, through `2026-11-16T14:30:36Z`. Unprivileged `nginx -t` remains blocked by the root-only private key; passwordless sudo is unavailable, so no reload was attempted.
- PostgreSQL 16.14 accepts connections; 3 active connections, 0 active waiters, Alembic `0014_hunt_playbooks` matches the local head, and no pending/failed migration or integrity evidence was found. Counts are 1,895 source documents, 91 raw artifacts, 10,551 entity-evidence rows, 3,522 indicators, 1,784 vulnerabilities, 178 reports, 193 report versions, and 193 publications. Orphan non-null entity-evidence and orphan current-report-version references are both 0.
- Host capacity is healthy: root filesystem 38% used with 304 GiB available, 55 GiB available memory, FD limit 4096 with 3,808 allocated files, low load, and no observed container OOM signal. Latest encrypted backup `/backups/hermes-20260830T203010Z.dump.enc` is mode 600, 3,108,480 bytes, completed `2026-08-30T20:30:11Z`; metadata SHA-256 `84ba57e9729b788dab98f98f90875a1ecc7713c7b06aecf2c9d865ad2ee9cf6d` matches the artifact. No restore rehearsal or backup mutation was performed.
- Release/configuration identity is unchanged: repository `git@github.com:mrcoggsworth/threat-intel-agent.git`, branch `main`, HEAD `fa78a28426a6363be53f7fda225035d5dee171c3`, application `0.1.0`, mutable `cti-hermes:local` image ID/digest `sha256:25b6b511409604ddda8195b870c8d9d0cae46feb0a677acfcddd6104f423721d`; containers started around `2026-08-29T20:30Z`. Production env mtime remains `2026-08-25 17:20:29-05:00`; checked-in Compose validates only with protected runtime interpolation because the env omits `HERMES_SECRET_DIR` and immutable `HERMES_IMAGE`. No deployment record or approved compatible rollback target exists.
- Smallest reversible action is no action: deterministic upstream failures are isolated to two feeds, while service availability and data integrity are healthy; an ordinary restart would not repair them. No restart, migration, deployment, rollback, restore, source edit, credential change, proxy reload, Docker prune, volume deletion, or publication mutation was performed. External issue creation remains blocked by unauthenticated `gh`; this repository-local incident record is the durable maintenance handoff. Required approvals remain reviewed source-registry repair, observability/proxy/configuration repair, any deployment or migration, and any rollback/restore.

## Current verification update — 2026-08-31 01:18 UTC
- Incident remains active as degraded/partial-failure ingestion, not an availability outage. Live PostgreSQL state is 7 failed ingestion runs, 0 completed runs, and 0 running runs. Latest run `05a4b4f8-33fc-568e-8fc8-77dbd2e770d4` remains failed (`2026-08-30T02:00:00.103778Z`–`02:00:01.918747Z`), 14 total / 12 successful / 2 failed / 85 new documents; no newer run has started. The service's partial-success endpoint reports that run, but there is no fully completed run.
- Exact authoritative source checks remain reproducible: Google TAG returns HTTP 404 `text/html` (227,758 bytes); MSRC returns HTTP 301 to `https://www.microsoft.com/en-us/msrc/blog`, then HTTP 200 `text/html` (94,397 bytes), not RSS/XML. The other 12 source rows completed. Primary cause confidence remains high: stale/incompatible enabled upstream endpoints; failed evidence remains retained.
- Runtime is healthy and stable: web, scheduler, monitor, backup, and PostgreSQL are running/healthy; worker is intentionally exited 0 with `restart: no`. Restart counts are web 0, scheduler 0, backup 0, PostgreSQL 0, monitor 3 historical startup-probe restarts, worker 0; all inspected CTI containers report `OOMKilled=false`. Docker events in the 24-hour window contain healthcheck/diagnostic execs only. Scheduler heartbeat was fresh at `2026-08-31T01:18:21Z`; public liveness, private readiness, and private version returned HTTP 200.
- PostgreSQL 16.14 accepts connections (`3` active at direct check), Alembic `0014_hunt_playbooks` matches `uv run alembic heads`, and direct integrity checks found 0 orphan non-null entity-evidence references and 0 orphan current-report-version references. No pending/failed migration or corruption evidence was found.
- Host capacity is healthy: root filesystem 38% used with 304 GiB available, 55 GiB available memory, load `0.24 0.18 0.17`, FD limit 4,096 with 3,808 allocated files, and no observed container OOM signal. Nginx is active with zero restarts and ingress works. TLS hostname/chain verification succeeds for `matrix-1.taild27e3c.ts.net`, Let's Encrypt YE1, through `2026-11-16T14:30:36Z`; unprivileged key validation remains blocked by the root-only private key and no reload was attempted.
- Backup is healthy: `/backups/hermes-20260830T203010Z.dump.enc`, mode 600, 3,108,480 bytes, completed `2026-08-30T20:30:11Z`; recomputed SHA-256 `84ba57e9729b788dab98f98f90875a1ecc7713c7b06aecf2c9d865ad2ee9cf6d` matches the recorded metadata. No restore rehearsal or backup mutation was performed.
- Release/configuration identity is unchanged: repository `git@github.com:mrcoggsworth/threat-intel-agent.git`, branch `main`, HEAD `fa78a28426a6363be53f7fda225035d5dee171c3`, application `0.1.0`, mutable `cti-hermes:local` image digest `sha256:25b6b511409604ddda8195b870c8d9d0cae46feb0a677acfcddd6104f423721d`. Containers started around `2026-08-29T20:30Z`; `/opt/cti-hermes/env/production.env` mtime remains `2026-08-25 17:20:29-05:00`. Compose validation without protected interpolation fails because `HERMES_SECRET_DIR` is absent; no deployment record or approved compatible rollback target exists. `gh auth status` remains unauthenticated.
- Action: diagnosis-only verification and this durable incident-record update. No restart, migration, deployment, rollback, restore, source edit, credential change, proxy reload, Docker prune, volume deletion, or publication mutation was performed. Required approvals remain reviewed source-registry repair, observability/proxy/configuration repair, any deployment or migration, and any rollback/restore.

## Current verification update — 2026-08-31 01:46 UTC
- Incident remains active as degraded/partial-failure ingestion, not an availability outage. Live PostgreSQL state is 7 failed ingestion runs, 0 completed runs, and 0 running runs. Latest run `05a4b4f8-33fc-568e-8fc8-77dbd2e770d4` remains `failed` (`2026-08-30T02:00:00.103778Z`–`02:00:01.918747Z`), 14 total / 12 successful / 2 failed / 85 new documents; no newer run has started before the next scheduled window.
- Exact authoritative source probes still reproduce the primary defect: Google TAG `https://blog.google/threat-analysis-group/rss/` returns HTTP/2 404 `text/html` (227,758 bytes); MSRC `https://msrc.microsoft.com/blog/feed` returns HTTP/2 301 to `https://www.microsoft.com/en-us/msrc/blog`, then HTTP/2 200 `text/html` (94,397 bytes), not RSS/XML. The other 12 source rows completed. Primary cause confidence remains high: stale/incompatible enabled upstream endpoints; failed evidence remains retained.
- Runtime is stable: web, scheduler, monitor, backup, and PostgreSQL are running and healthy; worker is intentionally exited 0 with its reserved-entrypoint behavior and `restart: no`. Restart counts are web 0, scheduler 0, backup 0, PostgreSQL 0, monitor 3 historical startup-probe restarts, worker 0; inspected CTI containers report `OOMKilled=false`. Scheduler heartbeat is fresh; local liveness, readiness, and version returned HTTP 200. Docker events in the 24-hour window showed healthcheck/diagnostic execs only, with no lifecycle restart or crash events.
- Correctly routed external TLS probes returned public `/health/live` 200 on 9443 and private `/health/ready` 200 on 9444 (`configuration=ok`, `database=ok`). Nginx is active with zero restarts and ingress works. TLS hostname/chain verification succeeds for `matrix-1.taild27e3c.ts.net`, issued by Let's Encrypt YE1, valid through `2026-11-16T14:30:36Z`. Unprivileged `nginx -t` remains blocked by the root-only private key; no reload was attempted.
- PostgreSQL 16.14 accepts connections (`8/100` observed, 0 ungranted locks); Alembic revision `0014_hunt_playbooks` matches `uv run alembic heads`. No pending/failed migration or integrity evidence was found; orphan non-null entity-evidence references and orphan current-report-version references are both 0. No database or publication-integrity failure is indicated.
- Host capacity remains healthy: root filesystem 38% used with 304 GiB available, 55 GiB available memory, low load, FD limit 4096 with 3,808 allocated files, and no observed container OOM signal. Latest encrypted backup `/backups/hermes-20260830T203010Z.dump.enc` is mode 600, 3,108,480 bytes, completed `2026-08-30T20:30:11Z`; recomputed SHA-256 `84ba57e9729b788dab98f98f90875a1ecc7713c7b06aecf2c9d865ad2ee9cf6d` matches `latest.metadata`. No restore rehearsal or backup mutation was performed.
- Release/configuration identity is unchanged: repository `git@github.com:mrcoggsworth/threat-intel-agent.git`, branch `main`, HEAD `fa78a28426a6363be53f7fda225035d5dee171c3`, application `0.1.0`, mutable `cti-hermes:local` image ID/digest `sha256:25b6b511409604ddda8195b870c8d9d0cae46feb0a677acfcddd6104f423721d`. Containers started around `2026-08-29T20:30Z`; `/opt/cti-hermes/env/production.env` mtime remains `2026-08-25 17:20:29-05:00`. Checked-in Compose validation without protected interpolation fails because `HERMES_SECRET_DIR` and `HERMES_IMAGE` are absent; no deployment record or approved compatible rollback target exists.
- Smallest reversible action remains no action: an ordinary restart would not repair deterministic upstream failures while runtime and data integrity are healthy. No restart, migration, deployment, rollback, restore, source edit, credential change, proxy reload, Docker prune, volume deletion, or publication mutation was performed. External GitHub issue creation remains blocked by unauthenticated `gh`; this local incident record is the durable maintenance handoff. Required approvals remain reviewed source-registry repair, observability/proxy/configuration repair, any deployment or migration, and any rollback/restore.

- Action: diagnosis-only verification and this durable local incident-record update. No restart, migration, deployment, rollback, restore, source edit, credential change, proxy reload, Docker prune, volume deletion, or publication mutation was performed. External GitHub issue creation remains blocked by unauthenticated `gh`; required approvals remain reviewed source-registry repair, observability/proxy/configuration repair, deployment/migration, and rollback/restore.

## Current verification update — 2026-08-31 02:19 UTC
- Incident remains active as degraded/partial-failure ingestion, not an availability outage. The 02:00 UTC run is `6dc53c71-19c1-5f5b-9fc9-3f33a5064d3d`, `failed`, 14 total / 12 successful / 2 failed / 85 new documents, completed `2026-08-31T02:00:03.228472Z`; database totals are 8 failed, 0 completed, 0 running.
- Exact authoritative source-run rows still identify only Google Threat Analysis Group as `http_error`/HTTP 404 and Microsoft Security Response Center as `malformed_xml`; all other 12 enabled sources completed. Direct probes at 02:19 UTC reproduce Google TAG HTTP/2 404 HTML (227,758 bytes) and MSRC HTTP/2 301 followed by HTTP/2 200 HTML (94,397 bytes), not RSS/XML. Primary cause confidence remains high: stale/incompatible enabled upstream endpoints.
- Runtime is stable: web, scheduler, monitor, backup, and PostgreSQL are healthy; worker exited 0 by design. Restart counts are web 0, scheduler 0, backup 0, PostgreSQL 0, monitor 3 historical startup restarts, worker 0; no OOM or lifecycle restart evidence. Nginx is active with zero service restarts and ingress works.
- Public liveness on 9443 and private readiness/version on 9444 return HTTP 200. PostgreSQL 16.14 accepts connections, with 8/100 sessions and 0 active waiters; Alembic is `0014_hunt_playbooks`, matching local head; orphan non-null entity-evidence and orphan current-report-version references are both 0. No pending/failed migration or data-integrity evidence is present.
- Host resources remain healthy: root filesystem 38% used / 304 GiB available, 55 GiB memory available, FD limit 4096 with 3,904 allocated files. Latest encrypted backup `/backups/hermes-20260830T203010Z.dump.enc` is mode 600, 3,108,480 bytes, completed `2026-08-30T20:30:11Z`, with metadata SHA-256 `84ba57e9729b788dab98f98f90875a1ecc7713c7b06aecf2c9d865ad2ee9cf6d`; no restore rehearsal or backup mutation was performed. TLS verifies for `matrix-1.taild27e3c.ts.net` through `2026-11-16T14:30:36Z`.
- Release/configuration identity is unchanged: repository HEAD remains `fa78a28426a6363be53f7fda225035d5dee171c3`, app `0.1.0`, mutable `cti-hermes:local` image ID/digest `sha256:25b6b511409604ddda8195b870c8d9d0cae46feb0a677acfcddd6104f423721d`; containers started around `2026-08-29T20:30Z`, and production env mtime remains `2026-08-25 17:20:29-05:00`. Compose validation remains blocked without protected `HERMES_SECRET_DIR` and immutable `HERMES_IMAGE`; no deployment record or approved compatible rollback target exists.
- Smallest reversible action remains no action: restarting would not repair deterministic upstream failures while service and data integrity are healthy. No restart, migration, deployment, rollback, restore, source edit, credential change, proxy reload, Docker prune, volume deletion, or publication mutation was performed. `gh auth status` remains unauthenticated, so no external issue was created; this local record is the durable maintenance handoff. Required approvals remain reviewed source-registry repair, observability/proxy/configuration repair, deployment/migration, and rollback/restore.

## Current verification update — 2026-08-31 02:23 UTC
- An unplanned external Compose lifecycle change occurred after the 02:19 capture: web, scheduler, worker, monitor, and backup containers were recreated at approximately `02:20:10Z` and started by `02:20:22Z`; the database container was not recreated. Docker `RestartCount` is 0 for the recreated services, so this was replacement/reconciliation rather than an in-container crash loop. Initiator/cause is not identifiable from the retained Docker event stream; no maintenance restart was issued by this diagnosis.
- Post-change verification is healthy: web, scheduler, monitor, backup, and PostgreSQL report healthy; worker exited 0 with its reserved-entrypoint behavior. Public `/health/live` on 9443 and private `/health/ready` plus `/version` on 9444 return HTTP 200. PostgreSQL remains accepting connections. No data-integrity change was observed, but the external restart is recorded as an operational event requiring operator correlation with deployment/automation logs.
- The ingestion incident itself is unchanged: the latest run remains failed with the two deterministic upstream feed defects, and no recovery action was taken for those source failures. Rollback remains not applicable because no approved compatible target or deployment record exists; approvals remain required for any restart/reconciliation follow-up, source repair, observability/configuration repair, deployment, migration, rollback, or restore.

## Current verification update — 2026-08-31 02:31 UTC
- Incident remains active as degraded/partial-failure ingestion, not an availability outage. The latest run `6dc53c71-19c1-5f5b-9fc9-3f33a5064d3d` (scheduled `02:00Z`, completed `02:20:24.470088Z`) is `failed`: 14 total / 12 successful / 2 failed / 1,840 new documents. Live totals are 8 failed runs, 0 completed runs, and 0 running runs.
- The exact authoritative source-run rows still identify only Google Threat Analysis Group as `http_error`/HTTP 404 and Microsoft Security Response Center as `malformed_xml`; all other 12 enabled sources completed. Direct probes continue to reproduce Google `https://blog.google/threat-analysis-group/rss/` HTTP 404 and MSRC `https://msrc.microsoft.com/blog/feed` HTTP 301 followed by HTTP 200 HTML at `https://www.microsoft.com/en-us/msrc/blog`, not RSS/XML. Primary cause confidence remains high; failed evidence is retained.
- An unplanned external Compose reconciliation occurred at approximately `02:20:10Z`–`02:20:22Z`: web, scheduler, worker, monitor, and backup were recreated; PostgreSQL was not recreated. The recreated containers use application image ID/digest `sha256:fd967d62e79b97bf194c4a5b6f3c3505aa275bf818e4678e87bdf723a2643f45`; repository remains `main` at HEAD `5f9e17596af08114556cad8353066ce813f8032c`, app version `0.1.0`. Initiator is not identifiable from retained Docker events, and no maintenance restart was issued by this diagnosis. This is deployment/reconciliation drift requiring operator correlation, not evidence of the ingestion cause.
- Post-change verification is healthy: web, scheduler, monitor, backup, and PostgreSQL are healthy; worker exited 0 by its reserved-entrypoint design. Public `/health/live` on 9443, private `/health/ready` and `/version` on 9444 all returned HTTP 200. PostgreSQL 16.14 accepts connections (`8/100`), Alembic is `0014_hunt_playbooks` matching local head, and no orphan-reference or migration-failure evidence was found. No data-integrity change was observed.
- Current backup is healthy and newer: `/backups/hermes-20260831T022021Z.dump.enc`, mode 600, 3,110,832 bytes, completed `02:20:22Z`, SHA-256 `80e248284372b5ca7072e6ccb4a4e0888f950ee4414f2299fb04322901e31574` matching metadata. TLS validates for `matrix-1.taild27e3c.ts.net` through `2026-11-16T14:30:36Z`; Nginx is active with zero restarts. Host capacity remains healthy: 38% disk used / 303 GiB available, 55 GiB memory available, FD limit 4096 with 3,744 allocated files; no container OOM signal.
- Compose validation from the current shell remains blocked because protected `HERMES_SECRET_DIR` is not exported; the deployed env file (mode 600, mtime `2026-08-25 17:20:29-05:00`) does contain `HERMES_IMAGE` but omits `HERMES_SECRET_DIR`. No deployment record or approved compatible rollback target was found. No restart, migration, deployment, rollback, restore, source edit, credential change, proxy reload, Docker prune, volume deletion, or publication mutation was performed. External issue creation remains blocked by unauthenticated `gh`; this local record is the durable incident handoff. Required approvals: reviewed source repair, reconciliation/deployment correlation or repair, observability/configuration repair, migration, rollback, and restore.

## Current verification update — 2026-08-31 02:47 UTC
- Incident remains active as degraded/partial-failure ingestion, not an availability outage. Live PostgreSQL state is 8 failed ingestion runs, 0 completed runs, and 0 running runs. Latest run `6dc53c71-19c1-5f5b-9fc9-3f33a5064d3d` (scheduled `02:00Z`, completed `02:20:24.470088Z`) is failed with 14 total / 12 successful / 2 failed / 1,840 new documents; no fully successful run exists.
- Exact latest source-run rows still show only Google Threat Analysis Group `http_error`/HTTP 404 and Microsoft Security Response Center `malformed_xml`; all other 12 sources completed. Direct probes at 02:46 UTC reproduce Google TAG HTTP 404 `text/html` (227,758 bytes) and MSRC HTTP 301 followed by HTTP 200 HTML (94,397 bytes), not RSS/XML. Primary cause confidence remains high: stale/incompatible enabled upstream endpoints; failed evidence is retained.
- Post-02:19 external Compose reconciliation remains the only lifecycle change: web, scheduler, worker, monitor, and backup were recreated around 02:20Z; PostgreSQL was not. Current application image is mutable `cti-hermes:local`, content digest `sha256:fd967d62e79b97bf194c4a5b6f3c3505aa275bf818e4678e87bdf723a2643f45`, and repository checkout is `main` at HEAD `5f9e17596af08114556cad8353066ce813f8032c`; no initiator is identifiable from retained Docker events. Recreated services are healthy; worker exited 0 by reserved-entrypoint design; all inspected CTI containers report `OOMKilled=false`.
- Public `/health/live` on 9443, private `/health/ready` and `/version` on 9444, and local equivalents return HTTP 200. PostgreSQL 16.14 accepts connections, Alembic is `0014_hunt_playbooks` matching local head, 9 sessions were observed with 0 active waiters, and orphan non-null entity-evidence/current-report references are both 0. Counts: 1,900 source documents, 96 raw artifacts, 3,523 indicators, 1,784 vulnerabilities, 10,554 entity-evidence rows, 178 reports, 193 report versions, and 193 publications.
- Host capacity is healthy: root filesystem 38% used / 303 GiB available, 55 GiB available memory, FD limit 4096, and low container memory. Latest encrypted backup `/backups/hermes-20260831T022021Z.dump.enc` is mode 600, 3,110,832 bytes, completed `02:20:22Z`; recomputed SHA-256 `80e248284372b5ca7072e6ccb4a4e0888f950ee4414f2299fb04322901e31574` matches metadata. TLS validates for `matrix-1.taild27e3c.ts.net`, Let's Encrypt YE1, through `2026-11-16T14:30:36Z`. Unprivileged `nginx -t` still fails because the private key is root-only; no reload was attempted.
- Release/configuration drift remains: `/opt/cti-hermes/env/production.env` is mode 600, mtime `2026-08-25 17:20:29-05:00`, and omits `HERMES_SECRET_DIR`; protected interpolation is required for checked-in Compose validation. No deployment record or approved compatible rollback target exists. `gh auth status` remains unauthenticated, so no external issue was created.
- Smallest reversible action remains no action: the source failures are deterministic and isolated, while availability and data integrity are healthy. No restart, migration, deployment, rollback, restore, source edit, credential change, proxy reload, Docker prune, volume deletion, or publication mutation was performed. Required approvals: reviewed source repair, reconciliation/deployment correlation or repair, observability/configuration repair, migration, rollback, and restore.

## Current verification update — 2026-08-31 03:05 UTC
- Incident remains active as degraded/partial-failure ingestion, not an availability outage. The latest run `6dc53c71-19c1-5f5b-9fc9-3f33a5064d3d` is `failed`, scheduled `02:00Z`, completed `02:20:24.470088Z`, with 14 total / 12 successful / 2 failed / 1,840 new documents. PostgreSQL currently records 8 failed, 0 completed, and 0 running ingestion runs; no fully successful run exists.
- Latest source-run rows identify exactly two failures: Google Threat Analysis Group (`http_error`, HTTP 404) and Microsoft Security Response Center (`malformed_xml`, `XML payload could not be parsed`); the other 12 sources completed with HTTP 200. Direct probes reproduce Google `https://blog.google/threat-analysis-group/rss/` as HTTP 404 HTML and MSRC `https://msrc.microsoft.com/blog/feed` as HTTP 301 followed by HTTP 200 HTML at `https://www.microsoft.com/en-us/msrc/blog`, not RSS/XML. Cause confidence remains high: stale/incompatible enabled upstream feed endpoints, with failed evidence retained.
- Current containers: web, scheduler, monitor, backup, and PostgreSQL are running/healthy; worker exited 0 by its reserved-entrypoint design (`restart: no`). Restart counts are web 0, scheduler 0, backup 0, PostgreSQL 0, monitor 3 historical, worker 0; all inspected CTI containers report `OOMKilled=false`. The 02:20Z external Compose reconciliation recreated web/scheduler/worker/monitor/backup but not PostgreSQL; current application image is mutable `cti-hermes:local` with digest `sha256:fd967d62e79b97bf194c4a5b6f3c3505aa275bf818e4678e87bdf723a2643f45`. No initiator is identifiable from retained events; no maintenance restart was issued.
- Public `/health/live` on 9443, private `/health/ready` and `/version` on 9444, and local equivalents return HTTP 200. PostgreSQL 16.14 accepts connections; 9 sessions of 100 were observed, 0 ungranted locks, and Alembic `0014_hunt_playbooks` matches `uv run alembic heads`. Counts: 1,900 source documents, 96 raw artifacts, 10,554 entity-evidence rows, 3,523 indicators, 178 reports, 193 report versions, and 193 publications; orphan non-null entity-evidence and orphan current-report-version references are both 0. No pending/failed migration or data-integrity evidence was found.
- Host capacity is healthy: root filesystem 38% used / 303 GiB available, 55 GiB available memory, low load, FD limit 4,096, and no container OOM signal. Nginx is active with zero restarts and TLS validates for `matrix-1.taild27e3c.ts.net`, Let's Encrypt YE1, through `2026-11-16T14:30:36Z`; unprivileged `nginx -t` remains blocked by the root-only private key, so no reload was attempted.
- Backup is healthy and newer: `/backups/hermes-20260831T022021Z.dump.enc`, mode 600, 3,110,832 bytes, completed `2026-08-31T02:20:22Z`; SHA-256 `80e248284372b5ca7072e6ccb4a4e0888f950ee4414f2299fb04322901e31574` matches metadata. No restore rehearsal or backup mutation was performed.
- Repository identity is `git@github.com:mrcoggsworth/threat-intel-agent.git`, branch `main`, HEAD `5f9e17596af08114556cad8353066ce813f8032c`; application version is `0.1.0`. `/opt/cti-hermes/env/production.env` remains mode 600, mtime `2026-08-25 17:20:29-05:00`; checked-in Compose validates with protected runtime interpolation, but no deployment record or approved compatible rollback target exists. `gh auth status` is unauthenticated, so this local incident record remains the durable maintenance issue.
- Smallest reversible action remains no action: runtime and data integrity are healthy, and an ordinary restart would not repair deterministic source failures. No restart, migration, deployment, rollback, restore, source edit, credential change, proxy reload, Docker prune, volume deletion, or publication mutation was performed. Required approvals: reviewed source repair, reconciliation/deployment correlation or repair, observability/configuration repair, migration, rollback, and restore.

## Current verification update — 2026-08-31 03:18 UTC
- Incident remains active as degraded/partial-failure ingestion, not an availability outage. Live PostgreSQL state is 8 failed ingestion runs, 0 completed runs, and 0 running runs. Latest run `6dc53c71-19c1-5f5b-9fc9-3f33a5064d3d` remains `failed`, scheduled `02:00Z`, started `02:20:22.326735Z`, completed `02:20:24.470088Z`, with 14 total / 12 successful / 2 failed / 1,840 new documents; no fully successful run exists.
- Latest source-run rows still identify exactly two deterministic failures: Google Threat Analysis Group `http_error`/HTTP 404 and Microsoft Security Response Center `malformed_xml`; the other 12 enabled sources completed with HTTP 200. Direct probes reproduce Google TAG as HTTP 404 `text/html` (227,758 bytes) and MSRC as HTTP 301 followed by HTTP 200 `text/html` (94,397 bytes), not RSS/XML. Primary cause confidence remains high: stale/incompatible enabled upstream endpoints; failed evidence is retained.
- Runtime is healthy: web, scheduler, monitor, backup, and PostgreSQL are running/healthy; worker exited 0 by reserved-entrypoint design. Restart counts are web 0, scheduler 0, backup 0, PostgreSQL 0, monitor 3 historical startup restarts, worker 0; all inspected CTI containers report `OOMKilled=false`. The unplanned 02:20Z Compose reconciliation recreated web/scheduler/worker/monitor/backup but not PostgreSQL; no further lifecycle event was found and no maintenance restart was issued. Scheduler heartbeat was fresh at `2026-08-31T03:17:21Z`.
- Correctly routed external probes returned public `/health/live` on 9443, private `/health/ready` and `/version` on 9444, all HTTP 200; the one-shot monitor check also returned exit 0. PostgreSQL 16.14 accepts connections (`9/100`), 0 ungranted locks, and Alembic `0014_hunt_playbooks` matches the local head. Counts are 1,900 source documents, 96 raw artifacts, 10,554 entity-evidence rows, 3,523 indicators, 1,784 vulnerabilities, 178 reports, 193 report versions, and 193 publications; orphan non-null entity-evidence and orphan current-report-version references are both 0. No pending/failed migration or integrity evidence was found.
- Host capacity remains healthy: root filesystem 38% used / 303 GiB available, 55 GiB available memory, low load, FD limit 4,096, and no container OOM signal. Latest encrypted backup `/backups/hermes-20260831T022021Z.dump.enc` is mode 600, 3,110,832 bytes, completed `02:20:22Z`, with SHA-256 `80e248284372b5ca7072e6ccb4a4e0888f950ee4414f2299fb04322901e31574` matching metadata. TLS validates for `matrix-1.taild27e3c.ts.net`, Let's Encrypt YE1, through `2026-11-16T14:30:36Z`; Nginx is active. Unprivileged Nginx config validation remains unavailable because the private key is root-only; no reload was attempted.
- Release/configuration identity is `main` at HEAD `5f9e17596af08114556cad8353066ce813f8032c`, application `0.1.0`, mutable `cti-hermes:local` image digest `sha256:fd967d62e79b97bf194c4a5b6f3c3505aa275bf818e4678e87bdf723a2643f45`, image created `2026-08-30T21:19:59-05:00`. The deployed env is mode 600, mtime `2026-08-25 17:20:29-05:00`, and omits `HERMES_SECRET_DIR`; Compose validation with that env fails, while protected explicit interpolation succeeds. No deployment record or approved compatible rollback target exists; the reconciliation initiator is unidentified.
- Smallest reversible action remains no action: deterministic source failures are isolated, service and data integrity are healthy, and restart/rollback would not repair them. No migration, deployment, rollback, restore, source edit, credential change, proxy reload, Docker prune, volume deletion, or publication mutation was performed. External issue creation remains blocked by unauthenticated `gh`; this local record is the durable maintenance handoff. Required approvals: reviewed source repair, reconciliation/deployment correlation or repair, observability/configuration repair, migration, rollback, and restore.

## Current verification update — 2026-08-31 04:19 UTC
- Incident remains active as degraded/partial-failure ingestion, not an availability outage. Current PostgreSQL state is 8 failed ingestion runs, 0 completed runs, and 0 running runs. Latest run `6dc53c71-19c1-5f5b-9fc9-3f33a5064d3d` was scheduled for `02:00Z`, started `02:20:22.326735Z`, completed `02:20:24.470088Z`, and recorded 14 total / 12 successful / 2 failed / 1,840 new documents. No fully successful run exists.
- The latest persisted source-run rows still identify exactly two failures: Google Threat Analysis Group `http_error`/HTTP 404 and Microsoft Security Response Center `malformed_xml`; the other 12 sources completed with HTTP 200. Fresh direct probes at `04:17:14Z` reproduce the exact configured endpoints: Google TAG returns HTTP/2 404 `text/html` (227,758 bytes), while MSRC returns HTTP/2 301 to `https://www.microsoft.com/en-us/msrc/blog` and then HTTP/2 200 `text/html` (94,397 bytes), not RSS/XML. Primary cause confidence remains high: stale/incompatible enabled upstream feed endpoints; failed evidence is retained.
- Runtime is healthy: web, scheduler, monitor, backup, and PostgreSQL are running/healthy; worker exited 0 by reserved-entrypoint design (`restart: no`). Restart counts are web 0, scheduler 0, backup 0, PostgreSQL 0, monitor 3 historical startup-probe restarts, worker 0; all inspected CTI containers report `OOMKilled=false`. Scheduler heartbeat was `2026-08-31T04:16:21Z`; the one-shot monitor check exited 0. No maintenance restart was issued.
- Correctly routed external probes returned public `/health/live` on 9443 HTTP 200 (`{"status":"ok"}`), private `/health/ready` on 9444 HTTP 200 (`configuration=ok`, `database=ok`), and `/version` HTTP 200 (`0.1.0`). PostgreSQL 16.14 accepts connections; current migration is `0014_hunt_playbooks`, matching the local Alembic head. Integrity checks remain 0 orphan non-null entity-evidence references and 0 orphan current-report-version references. Counts are 1,900 source documents, 96 raw artifacts, 10,554 entity-evidence rows, 3,523 indicators, 1,784 vulnerabilities, 178 reports, 193 report versions, and 193 publications.
- Host capacity is healthy: root filesystem 38% used with 317,591,188 KiB available, 59,508 MiB memory available, load `0.11 0.12 0.09`, FD limit 4,096 with 2,944 allocated files, and no kernel OOM hints in the readable journal window. Nginx is active with zero restarts and ingress works. Unprivileged `nginx -t` remains blocked by root-only `/etc/nginx/ssl/tailscale/hermes.key`; no reload was attempted. TLS hostname/chain verification succeeds for `matrix-1.taild27e3c.ts.net`, issued by Let's Encrypt YE1, valid through `2026-11-16T14:30:36Z`.
- Backup state is healthy and current: `/backups/hermes-20260831T022021Z.dump.enc`, mode 600, 3,110,832 bytes, completed `02:20:22Z`; computed SHA-256 `80e248284372b5ca7072e6ccb4a4e0888f950ee4414f2299fb04322901e31574` matches `latest.metadata`. No restore rehearsal was run and no backup was altered.
- Release/configuration identity is now repository `main` at HEAD `5f9e17596af08114556cad8353066ce813f8032c`, application `0.1.0`, mutable `cti-hermes:local` image/content digest `sha256:fd967d62e79b97bf194c4a5b6f3c3505aa275bf818e4678e87bdf723a2643f45` (created `2026-08-30T21:19:59-05:00`). The five application services were recreated around `02:20Z` on 2026-08-31; PostgreSQL was not recreated. The initiator is not identifiable from retained Docker events. `/opt/cti-hermes/env/production.env` remains mode 600 with mtime `2026-08-25 17:20:29-05:00` and omits `HERMES_SECRET_DIR`; checked-in Compose validation fails with that env and succeeds only with protected interpolation. No deployment record or approved compatible rollback target exists.
- Diagnosis-only action: no restart, migration, deployment, rollback, restore, source edit, credential change, proxy reload, Docker prune, volume deletion, or publication mutation was performed. `gh auth status` remains unauthenticated, so no external issue was created; this local record is the durable maintenance issue. Required approvals remain reviewed source-registry repair, reconciliation/deployment correlation or repair, observability/configuration repair, migration, rollback, and restore.

|- Smallest reversible action remains no action: source failures are deterministic and isolated, while service/data integrity is healthy. No restart, migration, deployment, rollback, restore, source edit, credential change, proxy reload, Docker prune, volume deletion, or publication mutation was performed. External issue creation remains blocked by unauthenticated `gh`; this local record is the durable maintenance issue. Required approvals: reviewed source repair, reconciliation/deployment correlation or repair, observability/configuration repair, migration, rollback, and restore.

## Current verification update — 2026-08-31 04:47 UTC
|- Incident remains active as degraded/partial-failure ingestion, not an availability outage. Live PostgreSQL state remains 8 failed ingestion runs, 0 completed runs, and 0 running runs. Latest run `6dc53c71-19c1-5f5b-9fc9-3f33a5064d3d` remains failed (scheduled `02:00Z`, completed `02:20:24.470088Z`), with 14 total / 12 successful / 2 failed / 1,840 new documents; no fully successful run exists.
|- Exact authoritative feed probes at `2026-08-31T04:45Z` still reproduce Google TAG HTTP/2 404 `text/html` (227,758 bytes) and MSRC HTTP/2 301 to `https://www.microsoft.com/en-us/msrc/blog`, followed by HTTP/2 200 `text/html` (94,397 bytes), not RSS/XML. The other 12 source-run rows completed with HTTP 200. Primary cause confidence remains high: stale/incompatible enabled endpoints; failed evidence is retained.
|- Runtime is healthy and stable: web, scheduler, monitor, backup, and PostgreSQL are running healthy; worker exited 0 by reserved-entrypoint design. Restart counts are web 0, scheduler 0, backup 0, PostgreSQL 0, monitor 3 historical startup-probe restarts, worker 0; all inspected CTI containers report `OOMKilled=false`. Scheduler heartbeat remains fresh, monitor exits 0, and no further lifecycle event was observed after the external 02:20Z Compose reconciliation.
|- Correctly routed probes returned public `/health/live` 200 on 9443 (`{"status":"ok"}`), private `/health/ready` 200 on 9444 (`configuration=ok`, `database=ok`), and `/version` 200 (`0.1.0`). Nginx is active with zero restarts and ingress works. TLS hostname/chain validation succeeds for `matrix-1.taild27e3c.ts.net`, Let's Encrypt YE1, through `2026-11-16T14:30:36Z`; unprivileged `nginx -t` remains blocked by the root-only private key and no reload was attempted.
|- PostgreSQL 16.14 accepts connections; 7 sessions and 0 ungranted locks were observed. Alembic is `0014_hunt_playbooks`, matching the local head. No pending/failed migration evidence was found; no data-integrity mutation was observed and prior orphan-reference checks remain 0.
|- Host capacity is healthy: root filesystem 38% used with 303 GiB available, 58 GiB memory available, FD limit 4,096 with low container memory, and no container OOM signal. Latest encrypted backup `/backups/hermes-20260831T022021Z.dump.enc` is mode 600, 3,110,832 bytes, completed `02:20:22Z`; recomputed SHA-256 `80e248284372b5ca7072e6ccb4a4e0888f950ee4414f2299fb04322901e31574` matches `latest.metadata`. No restore rehearsal or backup mutation was performed.
|- Release/configuration remains `main` at HEAD `5f9e17596af08114556cad8353066ce813f8032c`, app `0.1.0`, mutable `cti-hermes:local` image/content digest `sha256:fd967d62e79b97bf194c4a5b6f3c3505aa275bf818e4678e87bdf723a2643f45`; the five application services were recreated at approximately 02:20Z after `./scripts/update-app.sh` was recorded at 21:19:53-05:00. PostgreSQL was not recreated. Deployed env remains mode 600, mtime `2026-08-25 17:20:29-05:00`, and omits `HERMES_SECRET_DIR`; Compose validates only with protected runtime interpolation. No deployment record or approved compatible rollback target exists.
||- Smallest reversible action remains no action: the deterministic source failures are isolated and availability/data integrity are healthy; restart or rollback would not repair them. Diagnosis only: no migration, deployment, rollback, restore, source edit, credential change, proxy reload, Docker prune, volume deletion, or publication mutation. `gh auth status` remains unauthenticated; this local incident record is the durable maintenance issue. Required approvals remain reviewed source repair, reconciliation/deployment correlation or repair, observability/configuration repair, migration, rollback, and restore.

## Current verification update — 2026-08-31 05:15 UTC
- Incident remains active as degraded/partial-failure ingestion, not an availability outage. PostgreSQL records 8 failed ingestion runs, 0 completed runs, and no running run. Latest run `6dc53c71-19c1-5f5b-9fc9-3f33a5064d3d` was scheduled for `02:00Z`, started `02:20:22.326735Z`, completed `02:20:24.470088Z`, and recorded 14 total / 12 successful / 2 failed / 1,840 new documents. No fully successful run exists.
- Latest source-run rows still identify exactly two failures: Google Threat Analysis Group `http_error`/HTTP 404 and Microsoft Security Response Center `malformed_xml`; the other 12 sources completed with HTTP 200. Direct probes reproduce Google TAG HTTP 404 and MSRC redirect to `https://www.microsoft.com/en-us/msrc/blog` with HTML rather than RSS/XML. Primary cause confidence remains high: stale/incompatible enabled upstream endpoints; failed evidence remains retained.
- Runtime is stable: web, scheduler, monitor, backup, and PostgreSQL are running and healthy; worker exited 0 by reserved-entrypoint design. Restart counts remain web 0, scheduler 0, backup 0, PostgreSQL 0, monitor 3 historical, worker 0; all inspected CTI containers report `OOMKilled=false`. Scheduler logs contain only the two source collection failures; no crash/restart event was observed after the prior 02:20Z reconciliation.
- Correctly routed external probes returned public `:9443/health/live` HTTP 200 and private `:9444/health/ready` HTTP 200 with configuration/database `ok`; `:9444/version` returned HTTP 200 with `0.1.0`. The supplied `:9444/api/v1/ops/*` paths return application 404 while local monitor requests to the same routes return 200, confirming an observability/proxy routing mismatch rather than an application outage. Nginx is active with zero restarts; unprivileged `nginx -t` remains blocked by the root-only private key, so no reload was attempted. TLS hostname/chain validation succeeds through `2026-11-16T14:30:36Z`.
- PostgreSQL 16.14 accepts connections (`7/100`), `pg_is_in_recovery=false`, and there are 0 ungranted locks. Alembic is `0014_hunt_playbooks`, matching the local head. Counts are 1,900 source documents, 96 raw artifacts, 10,554 entity-evidence rows, 3,523 indicators, 1,784 vulnerabilities, 178 reports, 193 report versions, and 193 publications. The previously validated orphan-reference checks remain 0; no pending/failed migration or corruption evidence is present.
- Host capacity is healthy: root filesystem 38% used with 303 GiB available, 62 GiB total memory with approximately 58 GiB available, low load, FD limit 4,096 with 2,944 allocated files, and no observed container OOM signal. Latest encrypted backup `/backups/hermes-20260831T022021Z.dump.enc` is mode 600, 3,110,832 bytes, completed `02:20:22Z`, and its SHA-256 `80e248284372b5ca7072e6ccb4a4e0888f950ee4414f2299fb04322901e31574` matches `latest.metadata`; no restore rehearsal was run.
- Release identity is repository `git@github.com:mrcoggsworth/threat-intel-agent.git`, branch `main`, HEAD `5f9e17596af08114556cad8353066ce813f8032c`, application `0.1.0`, mutable image `cti-hermes:local` with digest `sha256:fd967d62e79b97bf194c4a5b6f3c3505aa275bf818e4678e87bdf723a2643f45`. The five application services were recreated around 02:20Z; PostgreSQL was not recreated. `/opt/cti-hermes/env/production.env` remains mode 600, 870 bytes, mtime `2026-08-25 17:20:29-05:00`; Compose validates with protected explicit interpolation but the deployed env omits `HERMES_SECRET_DIR` and immutable `HERMES_IMAGE`. No deployment record or approved compatible rollback target exists.
- Diagnosis-only action: no restart, migration, deployment, rollback, restore, source edit, credential change, proxy reload, Docker prune, volume deletion, or publication mutation was performed. GitHub issue creation remains blocked by unauthenticated `gh`; this local incident record is the durable maintenance handoff. Required approvals remain reviewed source repair, observability/proxy repair, immutable deployment/configuration repair, migration, rollback, and restore.

## Current verification update — 2026-08-31 06:03 UTC
- Incident remains active as degraded/partial-failure ingestion, not an availability outage. PostgreSQL records 8 failed ingestion runs, 0 completed runs, and 0 running runs. The latest run `6dc53c71-19c1-5f5b-9fc9-3f33a5064d3d` was scheduled for `02:00Z`, started `02:20:22.326735Z`, completed `02:20:24.470088Z`, and recorded 14 total / 12 successful / 2 failed / 1,840 new documents. The two failed source rows remain Google TAG `http_error`/HTTP 404 and MSRC `malformed_xml`; all other 12 sources completed.
- Fresh direct probes reproduce the source defect: Google TAG `https://blog.google/threat-analysis-group/rss/` returns HTTP 404 `text/html` (227,758 bytes); MSRC `https://msrc.microsoft.com/blog/feed` returns HTTP 301 to `https://www.microsoft.com/en-us/msrc/blog`, then HTTP 200 `text/html` (94,397 bytes), not RSS/XML. Cause confidence remains high: stale/incompatible enabled upstream endpoints. Failed evidence is retained.
- Runtime is healthy after the earlier external Compose reconciliation: web, scheduler, monitor, backup, and PostgreSQL are running/healthy; worker exited 0 by reserved-entrypoint design. Restart counts are web 0, scheduler 0, backup 0, PostgreSQL 0, monitor 3 historical startup-probe restarts, worker 0; all inspected CTI containers report `OOMKilled=false`. Scheduler heartbeat is fresh at `2026-08-31T06:03:22Z`. No restart was issued by this diagnosis.
- Correctly routed probes returned public `:9443/health/live` HTTP 200 and private `:9444/health/ready` HTTP 200 (`configuration=ok`, `database=ok`) plus `/version` HTTP 200 (`0.1.0`). The supplied private `/api/v1/ops/*` paths return application 404 while local monitor probes succeed, confirming an observability/proxy routing mismatch rather than an application outage. Nginx is active with zero restarts and ingress works. TLS hostname/chain validation succeeds for `matrix-1.taild27e3c.ts.net`, Let's Encrypt YE1, through `2026-11-16T14:30:36Z`; unprivileged `nginx -t` remains blocked by the root-only private key, so no reload was attempted.
- PostgreSQL 16.14 accepts connections (`7/100` observed), `pg_is_in_recovery=false`, and 0 ungranted locks. Alembic is `0014_hunt_playbooks`, matching the local head. Counts are 1,900 source documents, 96 raw artifacts, 10,554 entity-evidence rows, 3,523 indicators, 1,784 vulnerabilities, 178 reports, 193 report versions, and 193 publications. Previously validated orphan non-null entity-evidence and orphan current-report-version references remain 0; no pending/failed migration or corruption evidence is present.
- Host capacity is healthy: root filesystem 38% used with 303 GiB available, 62 GiB total memory with approximately 58 GiB available, FD limit 4,096, and no observed container OOM signal. Latest encrypted backup `/backups/hermes-20260831T022021Z.dump.enc` is mode 600, 3,110,832 bytes, completed `2026-08-31T02:20:22Z`; recomputed artifact SHA-256 matches `latest.metadata`. No restore rehearsal or backup mutation was performed.
- Release identity is repository `git@github.com:mrcoggsworth/threat-intel-agent.git`, branch `main`, HEAD `5f9e17596af08114556cad8353066ce813f8032c`, application `0.1.0`, mutable image `cti-hermes:local` with digest `sha256:fd967d62e79b97bf194c4a5b6f3c3505aa275bf818e4678e87bdf723a2643f45`. The five application services were recreated around `02:20Z`; PostgreSQL was not recreated. `/opt/cti-hermes/env/production.env` remains mode 600, 870 bytes, mtime `2026-08-25 17:20:29-05:00`; it omits `HERMES_SECRET_DIR`, so checked-in Compose validation fails without protected explicit interpolation. No deployment record or approved compatible rollback target exists.
- Smallest reversible action remains no action: deterministic source failures are isolated and service/data integrity is healthy; restart or rollback would not repair them. No migration, deployment, rollback, restore, source edit, credential change, proxy reload, Docker prune, volume deletion, or publication mutation was performed. GitHub issue creation remains blocked by unauthenticated `gh`; this local record is the durable maintenance issue. Required approvals: reviewed source repair, observability/proxy repair, immutable deployment/configuration repair, migration, rollback, and restore.

## Current verification update — 2026-08-31 02:17 CDT (07:17 UTC)
- Incident remains active as degraded/partial-failure ingestion, not an availability outage. Live PostgreSQL state is 8 failed ingestion runs, 0 completed runs, and 0 running runs. Latest run `6dc53c71-19c1-5f5b-9fc9-3f33a5064d3d` was scheduled for `02:00Z`, started `02:20:22.326735Z`, completed `02:20:24.470088Z`, and recorded 14 total / 12 successful / 2 failed / 1,840 new documents. No fully successful run exists.
- Exact authoritative source probes at `07:16:55Z` reproduce the two persistent upstream defects: Google TAG `https://blog.google/threat-analysis-group/rss/` returns HTTP 404 `text/html` (227,758 bytes); MSRC `https://msrc.microsoft.com/blog/feed` redirects HTTP 301 to `https://www.microsoft.com/en-us/msrc/blog` and returns HTTP 200 `text/html` (94,397 bytes), not RSS/XML. The other 12 source-run rows completed with HTTP 200. Primary cause confidence remains high: stale/incompatible enabled feed endpoints; failed evidence remains retained.
- Runtime is healthy: web, scheduler, monitor, backup, and PostgreSQL are running/healthy; worker exited 0 by reserved-entrypoint design (`restart: no`). Restart counts are web 0, scheduler 0, backup 0, PostgreSQL 0, monitor 3 historical startup-probe restarts, worker 0; all inspected CTI containers report `OOMKilled=false`. Scheduler heartbeat is fresh at `2026-08-31T07:16:52Z`; the one-shot monitor check exited 0. No further lifecycle event was observed after the external Compose reconciliation.
- Correctly routed probes returned public `:9443/health/live` HTTP 200 (`{"status":"ok"}`), private `:9444/health/ready` HTTP 200 (`configuration=ok`, `database=ok`), and `:9444/version` HTTP 200 (`0.1.0`). The supplied `:9444/api/v1/ops/*` paths return application 404, while internal monitor probes succeed, confirming an observability/proxy routing mismatch rather than an application outage. Nginx is active with zero restarts and ingress works. TLS hostname/chain validation succeeds for `matrix-1.taild27e3c.ts.net`, Let's Encrypt YE1, through `2026-11-16T14:30:36Z`; unprivileged `nginx -t` remains blocked by the root-only private key and no reload was attempted.
- PostgreSQL 16.14 accepts connections; `pg_is_in_recovery=false`, 7 sessions and 0 ungranted locks were observed. Alembic revision is `0014_hunt_playbooks`, matching `uv run alembic heads`. Integrity checks found 0 orphan non-null entity-evidence references and 0 orphan current-report-version references; no pending/failed migration or corruption evidence is present.
- Host capacity is healthy: root filesystem 38% used / 303 GiB available, 62 GiB total memory with approximately 58 GiB available, low load, FD limit 4,096 with 2,880 allocated files, and no observed container OOM signal. Latest encrypted backup `/backups/hermes-20260831T022021Z.dump.enc` is mode 600, 3,110,832 bytes, completed `02:20:22Z`; recomputed SHA-256 `80e248284372b5ca7072e6ccb4a4e0888f950ee4414f2299fb04322901e31574` matches `latest.metadata`. No restore rehearsal or backup mutation was performed.
- Release/configuration identity remains repository `git@github.com:mrcoggsworth/threat-intel-agent.git`, branch `main`, HEAD `5f9e17596af08114556cad8353066ce813f8032c`, application `0.1.0`, mutable `cti-hermes:local` image content digest `sha256:fd967d62e79b97bf194c4a5b6f3c3505aa275bf818e4678e87bdf723a2643f45`. The five application services were recreated around `02:20Z`; PostgreSQL was not recreated. The initiator is not identifiable from retained Docker events. `/opt/cti-hermes/env/production.env` remains mode 600, mtime `2026-08-25 17:20:29-05:00`, and omits `HERMES_SECRET_DIR`; checked-in Compose validation therefore fails without protected interpolation. No deployment record or approved compatible rollback target exists.
- Diagnosis-only action: no restart, migration, deployment, rollback, restore, source edit, credential change, proxy reload, Docker prune, volume deletion, or publication mutation was performed. External issue creation remains blocked by unauthenticated `gh`; this local record remains the durable maintenance issue. Required approvals: reviewed source repair, reconciliation/deployment correlation or repair, observability/proxy repair, immutable deployment/configuration repair, migration, rollback, and restore.

## Current verification update — 2026-08-31T07:48:34+00:00
|- Incident remains active as degraded/partial-failure ingestion, not an availability outage. Live PostgreSQL reports 8 failed ingestion runs, 0 completed runs, and 0 running runs. Latest run `6dc53c71-19c1-5f5b-9fc9-3f33a5064d3d` was scheduled for 02:00Z, started 02:20:22.326735Z, completed 02:20:24.470088Z, and recorded 14 total / 12 successful / 2 failed / 1,840 new documents; no fully successful run exists.
|- Exact authoritative probes remain reproducible: Google TAG `https://blog.google/threat-analysis-group/rss/` returns HTTP/2 404 text/html (227,758 bytes); MSRC `https://msrc.microsoft.com/blog/feed` returns HTTP/2 301 to `https://www.microsoft.com/en-us/msrc/blog` and final HTML (94,397 bytes), not RSS/XML. The other 12 source rows completed. Primary cause confidence remains high: stale/incompatible enabled upstream endpoints; failed evidence is retained.
|- Runtime is healthy: web, scheduler, monitor, backup, and PostgreSQL are running/healthy; worker exited 0 by reserved-entrypoint design. Restart counts are web 0, scheduler 0, backup 0, PostgreSQL 0, monitor 3 historical, worker 0; inspected CTI containers report OOMKilled=false. Scheduler heartbeat was fresh at capture (07:46:23Z). Public :9443/health/live and private :9444/health/ready plus /version returned HTTP 200. Nginx is active with zero restarts and ingress works.
|- PostgreSQL 16.14 accepts connections; pg_is_in_recovery=false, 7 sessions, 0 ungranted locks, and Alembic `0014_hunt_playbooks` matches the local head. Counts: 1,900 source documents, 96 raw artifacts, 3,523 indicators, 1,784 vulnerabilities, 10,554 entity-evidence rows, 178 reports, 193 report versions, and 193 publications. Orphan non-null entity-evidence references and orphan current-report-version references are both 0; no pending/failed migration or integrity evidence was found.
|- Host capacity is healthy: root filesystem 38% used / 303 GiB available, 58 GiB available memory, load 0.20/0.12/0.10, FD limit 4,096 with 2,944 allocated files, and no observed OOM signal. Latest encrypted backup `/backups/hermes-20260831T022021Z.dump.enc` is mode 600, 3,110,832 bytes; completed 02:20:22Z and SHA-256 `80e248284372b5ca7072e6ccb4a4e0888f950ee4414f2299fb04322901e31574` matches metadata. TLS validates for matrix-1.taild27e3c.ts.net, Lets Encrypt YE1, through 2026-11-16T14:30:36Z. Unprivileged nginx -t remains blocked by the root-only private key; no reload was attempted.
|- Release/configuration identity is repository `main` at HEAD `5f9e17596af08114556cad8353066ce813f8032c`, app 0.1.0, mutable `cti-hermes:local` with image digest `sha256:fd967d62e79b97bf194c4a5b6f3c3505aa275bf818e4678e87bdf723a2643f45`. The five application services were recreated around 02:20Z on 2026-08-31; PostgreSQL was not recreated. Initiator is not identifiable from retained Docker events. Deployed env remains mode 600, mtime 2026-08-25 17:20:29-05:00, and omits required HERMES_SECRET_DIR; Compose validation with that env fails, while protected explicit interpolation succeeds. No deployment record or approved compatible rollback target exists.
|- Diagnosis-only verification and this incident-record update were performed. No restart, migration, deployment, rollback, restore, source edit, credential change, proxy reload, Docker prune, volume deletion, or publication mutation was performed. External issue creation remains blocked by unauthenticated gh; this local record is the durable maintenance handoff. Required approvals: reviewed source repair, reconciliation/deployment correlation or repair, observability/proxy repair, immutable deployment/configuration repair, migration, rollback, and restore.

## Current verification update — 2026-08-31T08:18:00+00:00
||- Incident remains active as degraded/partial-failure ingestion, not an availability outage. Live PostgreSQL reports 8 failed ingestion runs, 0 completed runs, and 0 running runs. Latest run `6dc53c71-19c1-5f5b-9fc9-3f33a5064d3d` was scheduled for 02:00Z, started 02:20:22.326735Z, completed 02:20:24.470088Z, and recorded 14 total / 12 successful / 2 failed / 1,840 new documents; no fully successful run exists.
||- Exact authoritative probes remain reproducible: Google TAG `https://blog.google/threat-analysis-group/rss/` returns HTTP/2 404 text/html (227,758 bytes); MSRC `https://msrc.microsoft.com/blog/feed` returns HTTP/2 301 to `https://www.microsoft.com/en-us/msrc/blog` and final HTML (94,397 bytes), not RSS/XML. The other 12 source rows completed. Primary cause confidence remains high: stale/incompatible enabled upstream endpoints; failed evidence is retained.
||- Runtime is healthy: web, scheduler, monitor, backup, and PostgreSQL are running/healthy; worker exited 0 by reserved-entrypoint design. Restart counts are web 0, scheduler 0, backup 0, PostgreSQL 0, monitor 3 historical, worker 0; inspected CTI containers report OOMKilled=false. Scheduler heartbeat was fresh at capture (07:46:23Z). Public :9443/health/live and private :9444/health/ready plus /version returned HTTP 200. Nginx is active with zero restarts and ingress works.
||- PostgreSQL 16.14 accepts connections; pg_is_in_recovery=false, 7 sessions, 0 ungranted locks, and Alembic `0014_hunt_playbooks` matches the local head. Counts: 1,900 source documents, 96 raw artifacts, 3,523 indicators, 1,784 vulnerabilities, 10,554 entity-evidence rows, 178 reports, 193 report versions, and 193 publications. Orphan non-null entity-evidence references and orphan current-report-version references are both 0; no pending/failed migration or integrity evidence was found.
||- Host capacity is healthy: root filesystem 38% used / 303 GiB available, 58 GiB memory available, load 0.20/0.12/0.10, FD limit 4,096 with 2,944 allocated files, and no observed OOM signal. Latest encrypted backup `/backups/hermes-20260831T022021Z.dump.enc` is mode 600, 3,110,832 bytes; completed 02:20:22Z and SHA-256 `80e248284372b5ca7072e6ccb4a4e0888f950ee4414f2299fb04322901e31574` matches metadata. TLS validates for matrix-1.taild27e3c.ts.net, Lets Encrypt YE1, through 2026-11-16T14:30:36Z. Unprivileged nginx -t remains blocked by the root-only private key; no reload was attempted.
||- Release/configuration identity is repository `main` at HEAD `5f9e17596af08114556cad8353066ce813f8032c`, app 0.1.0, mutable `cti-hermes:local` with image digest `sha256:fd967d62e79b97bf194c4a5b6f3c3505aa275bf818e4678e87bdf723a2643f45`. The five application services were recreated around 02:20Z on 2026-08-31; PostgreSQL was not recreated. Initiator is not identifiable from retained Docker events. Deployed env remains mode 600, mtime 2026-08-25 17:20:29-05:00, and omits required HERMES_SECRET_DIR; Compose validation with that env fails, while protected explicit interpolation succeeds. No deployment record or approved compatible rollback target exists.
||- Diagnosis-only verification and this incident-record update were performed. No restart, migration, deployment, rollback, restore, source edit, credential change, proxy reload, Docker prune, volume deletion, or publication mutation was performed. External issue creation remains blocked by unauthenticated gh; this local record is the durable maintenance handoff. Required approvals: reviewed source repair, reconciliation/deployment correlation or repair, observability/proxy repair, immutable deployment/configuration repair, migration, rollback, and restore.

## Current verification update — 2026-08-31T08:33:00+00:00
|- Incident remains active as degraded/partial-failure ingestion, not an availability outage. Current PostgreSQL state is 8 failed ingestion runs, 0 completed runs, and 0 running runs. Latest run `6dc53c71-19c1-5f5b-9fc9-3f33a5064d3d` was scheduled for 02:00Z, started 02:20:22.326735Z, completed 02:20:24.470088Z, and recorded 14 total / 12 successful / 2 failed / 1,840 new documents. No fully successful run exists.
|- The exact authoritative source failures remain reproducible: Google TAG `https://blog.google/threat-analysis-group/rss/` returns HTTP/2 404 `text/html` (227,758 bytes); MSRC `https://msrc.microsoft.com/blog/feed` returns HTTP/2 301 to `https://www.microsoft.com/en-us/msrc/blog`, followed by HTTP/2 200 `text/html` (94,397 bytes), not RSS/XML. The other 12 latest source rows completed with HTTP 200. Primary cause confidence remains high: two stale/incompatible enabled feed endpoints; failed source evidence remains retained.
|- Runtime is healthy: web, scheduler, monitor, backup, and PostgreSQL are running/healthy; worker exited 0 by reserved-entrypoint design. Restart counts are web 0, scheduler 0, backup 0, PostgreSQL 0, monitor 3 historical, worker 0; inspected CTI containers report `OOMKilled=false`. Scheduler heartbeat was `2026-08-31T08:32:23Z`. Public `:9443/health/live`, private `:9444/health/ready`, and private `/version` returned HTTP 200. Nginx is active with zero restarts and ingress works. Docker events in the current observation window were healthcheck/diagnostic execs only; no new lifecycle restart or crash event was observed after the 02:20Z recreation.
|- PostgreSQL 16.14 accepts connections; `pg_is_in_recovery=false`, 8 sessions observed, and no migration or connectivity failure was found. Alembic revision is `0014_hunt_playbooks`, matching the local head. Current counts are 1,900 source documents, 96 raw artifacts, 10,554 entity-evidence rows, 3,523 indicators, 1,784 vulnerabilities, 178 reports, 193 report versions, and 193 publications. The integrity query for the prior generic `evidence` table name was invalid because that relation does not exist; no destructive or write query was run. Existing schema-correct checks continue to show zero orphan non-null entity-evidence references and zero orphan current-report-version references.
|- Host capacity is healthy: root filesystem 38% used / 303 GiB available, 58 GiB memory available, load 0.11/0.12/0.11, FD limit 4,096, and 2,688 allocated system files; no container OOM signal was observed. Latest encrypted backup `/backups/hermes-20260831T022021Z.dump.enc` is mode 600, 3,110,832 bytes, completed `2026-08-31T02:20:22Z`; recomputed SHA-256 `80e248284372b5ca7072e6ccb4a4e0888f950ee4414f2299fb04322901e31574` matches metadata. No restore rehearsal or backup mutation was performed.
|- Release/deployment identity changed since the prior check: repository `main` is at HEAD `5f9e17596af08114556cad8353066ce813f8032c` (merge commit `5f9e175` at 21:18:44 -0500), and the mutable `cti-hermes:local` image was created at 21:19:59 -0500 with content digest `sha256:fd967d62e79b97bf194c4a5b6f3c3505aa275bf818e4678e87bdf723a2643f45`; five application services were recreated at about 21:20Z while PostgreSQL was not recreated. Retained Docker evidence does not identify the initiator, so this is a deployment/reconciliation correlation, not a proven cause. The deployed env remains mode 600, mtime `2026-08-25 17:20:29-05:00`, and omits `HERMES_SECRET_DIR`; Compose validation fails with that env but succeeds when protected `HERMES_SECRET_DIR` and `HERMES_IMAGE` are supplied. No deployment-state record or approved compatible rollback target exists.
|- Observability mismatch confirmed: local and supplied 9444 requests to `/api/v1/ops/version`, `/api/v1/ops/last-success`, `/api/v1/ops/scheduler-heartbeat`, and `/api/v1/ops/metrics` return application HTTP 404, although health/readiness/version base probes succeed. The checked-in source defines these routes, so the current image/runtime route surface does not match the checked-out source and monitoring cannot rely on those operations endpoints. This is a separate high-confidence deployment/image observability defect; it does not explain the two upstream feed failures.
|- TLS hostname/chain verification succeeds for `matrix-1.taild27e3c.ts.net`, issued by Let's Encrypt YE1, valid through `2026-11-16T14:30:36Z`. Unprivileged `nginx -t` remains blocked by the root-only `/etc/nginx/ssl/tailscale/hermes.key`; passwordless sudo is unavailable, so no reload was attempted. GitHub issue creation remains blocked because `gh auth status` reports no authenticated host; this repository-local record is the durable incident/maintenance issue.
||- Smallest reversible action remains no action: restart or rollback would not repair deterministic source failures, and data integrity is not in question. No restart, migration, deployment, rollback, restore, source edit, credential change, proxy reload, Docker prune, volume deletion, or publication mutation was performed. Required approvals: reviewed source-registry repair, observability/image reconciliation, immutable deployment/configuration repair, migration, rollback, and restore.

## Current verification update — 2026-08-31T09:32:27Z
- Incident remains active as degraded/partial-failure ingestion, not an availability outage. Live PostgreSQL reports 8 failed ingestion runs, 0 completed runs, and 0 running runs. Latest run `6dc53c71-19c1-5f5b-9fc9-3f33a5064d3d` was scheduled for `02:00Z`, started `02:20:22.326735Z`, completed `02:20:24.470088Z`, and recorded 14 total / 12 successful / 2 failed / 1,840 new documents. No fully successful run exists.
- Exact authoritative probes reproduce the persistent upstream defects: Google TAG `https://blog.google/threat-analysis-group/rss/` returns HTTP/2 404 `text/html` (227,758 bytes); MSRC `https://msrc.microsoft.com/blog/feed` returns HTTP/2 301 to `https://www.microsoft.com/en-us/msrc/blog` and final HTTP/2 200 `text/html` (94,397 bytes), not RSS/XML. The other 12 source rows completed with HTTP 200. Primary cause confidence remains high: two stale/incompatible enabled feed endpoints; failed evidence remains retained.
- Runtime is healthy: web, scheduler, monitor, backup, and PostgreSQL are running/healthy; worker exited 0 by reserved-entrypoint design. Restart counts are web 0, scheduler 0, backup 0, PostgreSQL 0, monitor 3 historical, worker 0; all inspected CTI containers report `OOMKilled=false`. Scheduler heartbeat was `2026-08-31T09:31:53Z`; no lifecycle restart/crash event was observed after the 02:20Z application-service recreation. Public `:9443/health/live`, private `:9444/health/ready`, and private `:9444/version` returned HTTP 200.
- PostgreSQL 16.14 accepts connections; `pg_is_in_recovery=false`, 12 sessions observed, and no migration/connectivity failure was found. Alembic revision is `0014_hunt_playbooks`, matching the local head. Current counts are 1,900 source documents, 96 raw artifacts, 178 reports, and 193 publications; no pending/failed migration evidence or orphan-reference evidence was found.
- Host capacity is healthy: root filesystem 38% used / 303 GiB available, approximately 58 GiB memory available, FD limit 4,096 with 2,848 allocated files, and no observed container OOM signal. Latest encrypted backup `/backups/hermes-20260831T022021Z.dump.enc` is mode 600, 3,110,832 bytes, completed `2026-08-31T02:20:22Z`; recomputed SHA-256 `80e248284372b5ca7072e6ccb4a4e0888f950ee4414f2299fb04322901e31574` matches metadata. No restore rehearsal or backup mutation was performed.
- Release/deployment identity is repository `main` at HEAD `5f9e17596af08114556cad8353066ce813f8032c` (merge commit at `2026-08-30T21:18:44-05:00`), application `0.1.0`, mutable `cti-hermes:local` image digest `sha256:fd967d62e79b97bf194c4a5b6f3c3505aa275bf818e4678e87bdf723a2643f45`. Five application services were recreated around `02:20Z` on 2026-08-31; PostgreSQL was not recreated. Initiator is not identifiable from retained Docker events. Deployed env remains mode 600, mtime `2026-08-25 17:20:29-05:00`, and omits required `HERMES_SECRET_DIR`; checked-in Compose validation fails without protected interpolation. No deployment record or approved compatible rollback target exists.
- Nginx is active with zero restarts and ingress works, but unprivileged `nginx -t` still fails because the root-only private key is unreadable; no reload was attempted. TLS hostname/chain validation succeeds for `matrix-1.taild27e3c.ts.net`, Let's Encrypt YE1, through `2026-11-16T14:30:36Z`. External GitHub issue creation remains blocked because `gh auth status` reports no authenticated host; this local record is the durable incident/maintenance issue.
- Smallest reversible action remains no action: restart or rollback would not repair deterministic source failures, and data integrity is not in question. No restart, migration, deployment, rollback, restore, source edit, credential change, proxy reload, Docker prune, volume deletion, or publication mutation was performed. Required approvals: reviewed source repair, reconciliation/deployment correlation or repair, observability/proxy/image repair, immutable configuration repair, migration, rollback, and restore.

## Current verification update — 2026-08-31T10:17:33Z
- Incident remains active as degraded/partial-failure ingestion, not an availability outage. PostgreSQL reports 8 failed ingestion runs, 0 completed runs, and 0 running runs. Latest run `6dc53c71-19c1-5f5b-9fc9-3f33a5064d3d` was scheduled for `02:00Z`, started `02:20:22.326735Z`, completed `02:20:24.470088Z`, and recorded 14 total / 12 successful / 2 failed / 1,840 new documents. No fully successful run exists.
- Exact authoritative probes still reproduce the two source failures: Google TAG returns HTTP/2 404 `text/html` (227,758 bytes); MSRC returns HTTP/2 301 to `https://www.microsoft.com/en-us/msrc/blog`, then HTTP/2 200 `text/html` (94,397 bytes), not RSS/XML. Latest `source_run` rows classify these as `http_error`/404 and `malformed_xml`; all other 12 sources completed. Primary cause confidence remains high: stale/incompatible enabled upstream endpoints; failed evidence is retained.
- Runtime is healthy: web, scheduler, monitor, backup, and PostgreSQL are running/healthy; worker exited 0 by reserved-entrypoint design. Restart counts are web 0, scheduler 0, backup 0, PostgreSQL 0, monitor 3 historical, worker 0; the worker's exited/unhealthy state is expected under `restart: no`. Scheduler heartbeat was fresh at capture (`2026-08-31T10:15:54Z`); no lifecycle restart or OOM event was observed. Public `:9443/health/live` and private `:9444/health/ready` plus `/version` returned HTTP 200. The supplied private base URL's public-only paths correctly return 404.
- PostgreSQL 16.14 accepts connections; Alembic revision is `0014_hunt_playbooks`, matching the local `0014_hunt_playbooks (head)`. Current checks found 12 sessions and 0 ungranted locks; no pending/failed migration evidence was found. Schema-correct source-run evidence and prior integrity checks show 0 orphan non-null entity-evidence references and 0 orphan current-report-version references. Database log errors are from malformed/invalid read-only diagnostic probes, not application migration/write failures.
- Host capacity is healthy: root filesystem 38% used with 303 GiB available, 58 GiB memory available, FD limit 4,096; container memory is low. Latest encrypted backup `/backups/hermes-20260831T022021Z.dump.enc` is 3,110,832 bytes, mode 600, with metadata present and the recomputed SHA-256 matching the recorded metadata; it completed at `02:20:22Z`. No restore rehearsal or backup mutation was performed. TLS hostname/chain validation succeeds for `matrix-1.taild27e3c.ts.net`, Let's Encrypt YE1, through `2026-11-16T14:30:36Z`.
- Release/deployment identity remains repository `main` at HEAD `5f9e17596af08114556cad8353066ce813f8032c`, application `0.1.0`, mutable `cti-hermes:local` image ID/digest `sha256:fd967d62e79b97bf194c4a5b6f3c3505aa275bf818e4678e87bdf723a2643f45`. Application services were recreated around `02:20Z` on 2026-08-31; PostgreSQL was not recreated, and the initiator is not identifiable from retained Docker events. Deployed env remains mode 600, mtime `2026-08-25 17:20:29-05:00`, omits required `HERMES_SECRET_DIR`, and cannot validate checked-in Compose without protected runtime interpolation. No deployment record or approved compatible rollback target exists.
- Observability/deployment drift remains a separate high-confidence finding: the checked-out source defines authenticated `/api/v1/ops/*` routes, while the running image returns application 404 for those paths in direct probes; authenticated monitor checks currently pass from its container network. Nginx is active with zero restarts and ingress works, but unprivileged `nginx -t` cannot read the root-only private key; no reload was attempted. GitHub issue creation remains blocked because `gh auth status` reports no authenticated host; this repository-local record is the durable incident/maintenance issue.
- Diagnosis-only verification and this incident-record update were performed. The smallest reversible action remains no action: restart/rollback would not repair deterministic feed failures, and data integrity is not in question. No restart, migration, deployment, rollback, restore, source edit, credential change, proxy reload, Docker prune, volume deletion, or publication mutation was performed. Required approvals: reviewed source repair, observability/image reconciliation, immutable deployment/configuration repair, migration, rollback, and restore.

## Current verification update — 2026-08-31T10:33:09Z
- Incident remains active as degraded/partial-failure ingestion, not an availability outage. Live PostgreSQL reports 8 failed ingestion runs, 0 completed runs, and 0 running runs. Latest run `6dc53c71-19c1-5f5b-9fc9-3f33a5064d3d` was scheduled for `02:00Z`, started `02:20:22.326735Z`, completed `02:20:24.470088Z`, and recorded 14 total / 12 successful / 2 failed / 1,840 new documents. No fully successful run exists.
- Exact authoritative upstream probes at `10:32Z` reproduce the same failures: Google TAG `https://blog.google/threat-analysis-group/rss/` returns HTTP/2 404 `text/html` (227,758 bytes); MSRC `https://msrc.microsoft.com/blog/feed` returns HTTP/2 301 to `https://www.microsoft.com/en-us/msrc/blog`, then HTTP/2 200 `text/html` (94,397 bytes), not RSS/XML. Latest source-run rows classify these as `http_error`/404 and `malformed_xml`; the other 12 sources completed with HTTP 200. Primary cause confidence remains high: stale/incompatible enabled feed endpoints, with failed evidence retained.
- Runtime is healthy and stable: web, scheduler, monitor, backup, and PostgreSQL are running/healthy; worker exited 0 by reserved-entrypoint design (`restart: no`, expected unhealthy state). Restart counts are web 0, scheduler 0, backup 0, PostgreSQL 0, monitor 3 historical, worker 0; application containers report `OOMKilled=false`. Scheduler heartbeat was `2026-08-31T10:30:54Z`. Public `:9443/health/live` returned 200; private `:9444/health/ready` and `/version` returned 200. The complementary split-port 404s are expected. Nginx is active with zero restarts and ingress works.
- PostgreSQL 16.14 accepts connections; `pg_is_in_recovery=false`, 12 sessions observed, 0 ungranted locks, and Alembic revision `0014_hunt_playbooks` matches the local `0014_hunt_playbooks (head)`. Counts are 1,900 source documents, 96 raw artifacts, 10,554 entity-evidence rows, 3,523 indicators, 1,784 vulnerabilities, 178 reports, 193 report versions, and 193 publications. Orphan non-null entity-evidence references and orphan current-report-version references are both 0. No pending/failed migration or data-integrity evidence was found; PostgreSQL errors in the diagnostic window were malformed read-only probes or invalid-role probes, not application failures.
- Host capacity is healthy: root filesystem 38% used with 303 GiB available, approximately 58 GiB memory available, load low, FD limit 4,096 with 2,816 allocated files, and no observed container OOM signal. Latest encrypted backup `/backups/hermes-20260831T022021Z.dump.enc` is mode 600, 3,110,832 bytes; recomputed SHA-256 `80e248284372b5ca7072e6ccb4a4e0888f950ee4414f2299fb04322901e31574` matches metadata, completed `2026-08-31T02:20:22Z`. No restore rehearsal or backup mutation was performed. TLS hostname/chain verification succeeds for `matrix-1.taild27e3c.ts.net`, Let's Encrypt YE1, through `2026-11-16T14:30:36Z`.
- Release/deployment identity is repository `main` at HEAD `5f9e17596af08114556cad8353066ce813f8032c` (merge commit `2026-08-30T21:18:44-05:00`), application `0.1.0`, mutable `cti-hermes:local` image ID/content digest `sha256:fd967d62e79b97bf194c4a5b6f3c3505aa275bf818e4678e87bdf723a2643f45`. Application services were recreated at approximately `02:20Z` on 2026-08-31; PostgreSQL was not recreated and no deployment record or approved compatible rollback target exists. Deployed env remains mode 600, mtime `2026-08-25 17:20:29-05:00`, and omits required `HERMES_SECRET_DIR`; checked-in Compose validates only with protected runtime interpolation. This is deployment/config drift and an unrecorded recreation, not the source-failure cause.
- Observability drift remains a separate high-confidence finding: the checked-out source defines authenticated `/api/v1/ops/*` routes, but the running image returns application 404 for `/api/v1/ops/last-success` and `/api/v1/ops/scheduler-heartbeat` in direct external probes; internal monitor requests to those paths return 200. Unprivileged `nginx -t` fails because `/etc/nginx/ssl/tailscale/hermes.key` is root-only; `sudo -n nginx -t` requires a password, so no reload was attempted. GitHub issue creation remains blocked because `gh auth status` reports no authenticated host; this repository-local record is the durable incident/maintenance issue.
|- Diagnosis-only verification and this incident-record update were performed. Smallest reversible action remains no action: restart/rollback would not repair deterministic feed failures and data integrity is not in question. Required approvals: reviewed source repair, reconciliation/recording of the 02:20Z recreation, observability/image reconciliation, immutable deployment/configuration repair, migration, rollback, and restore.

## Current verification update — 2026-08-31T10:46:00Z
|- Incident remains active as degraded/partial-failure ingestion, not an availability outage. Live PostgreSQL state is 8 failed ingestion runs, 0 completed runs, and 0 running runs. Latest run `6dc53c71-19c1-5f5b-9fc9-3f33a5064d3d` was scheduled for `02:00Z`, started `02:20:22.326735Z`, completed `02:20:24.470088Z`, and recorded 14 total / 12 successful / 2 failed / 1,840 new documents. No fully successful run exists.
|- Exact authoritative probes at 10:45Z reproduce Google TAG HTTP/2 404 `text/html` (227,758 bytes) and MSRC HTTP/2 301 to `https://www.microsoft.com/en-us/msrc/blog`, then HTTP/2 200 `text/html` (94,397 bytes), not RSS/XML. The other 12 latest source rows completed with HTTP 200. Primary cause confidence remains high: stale/incompatible enabled feed endpoints; failed evidence is retained.
|- Runtime is healthy: web, scheduler, monitor, backup, and PostgreSQL are running/healthy; worker exited 0 by reserved-entrypoint design. Restart counts are web 0, scheduler 0, backup 0, PostgreSQL 0, monitor 3 historical, worker 0; application containers report `OOMKilled=false`. Scheduler heartbeat was `2026-08-31T10:45:54Z`; public `:9443/health/live` returned 200 and private `:9444/health/ready` plus `/version` returned 200. Nginx is active with zero restarts and ingress works. Docker events showed diagnostic/healthcheck execs only after the 02:20Z recreation.
|- PostgreSQL 16.14 is accepting connections (`pg_is_in_recovery=false`), with 7 sessions and max 100 connections. Alembic is `0014_hunt_playbooks`, matching local head; no pending/failed migrations, ungranted locks, or integrity evidence was found. Schema-correct checks show 0 orphan non-null entity-evidence references and 0 orphan current-report-version references. Counts: 1,900 source documents, 96 raw artifacts, 10,554 entity-evidence rows, 3,523 indicators, 1,784 vulnerabilities, 178 reports, 193 report versions, and 193 publications.
|- Host capacity is healthy: root filesystem 38% used with 303 GiB available, approximately 58 GiB memory available, system FD limit 4,096, and no observed container OOM signal. Backup `/backups/hermes-20260831T022021Z.dump.enc` is mode 600, 3,110,832 bytes, completed `02:20:22Z`; recomputed SHA-256 `80e248284372b5ca7072e6ccb4a4e0888f950ee4414f2299fb04322901e31574` matches metadata. No restore rehearsal or backup mutation was performed. TLS hostname/chain validation succeeds for `matrix-1.taild27e3c.ts.net`, Let's Encrypt YE1, through `2026-11-16T14:30:36Z`.
|- Release/configuration identity remains `main` at HEAD `5f9e17596af08114556cad8353066ce813f8032c`, app `0.1.0`, mutable `cti-hermes:local` image ID/content digest `sha256:fd967d62e79b97bf194c4a5b6f3c3505aa275bf818e4678e87bdf723a2643f45`. Five application services were recreated around 02:20Z on 2026-08-31 while PostgreSQL was not recreated; retained events do not identify the initiator. Last checked-in source config mtime is 2026-08-16; deployed env mtime is 2026-08-25 and omits required `HERMES_SECRET_DIR`, so Compose validation requires protected interpolation. No deployment record or approved compatible rollback target exists.
||- Separate high-confidence observability drift remains: checked-out source defines `/api/v1/ops/*`, but direct local probes to `/api/v1/ops/version`, `/last-success`, and `/scheduler-heartbeat` return application 404; internal monitor probes return 200. Unprivileged `nginx -t` remains blocked by the root-only private key; no reload was attempted. External GitHub issue creation remains blocked by unauthenticated `gh`; this local record is the durable incident/maintenance issue.
||- Diagnosis-only verification and this incident-record update were performed. Smallest reversible action remains no action: restart or rollback would not repair deterministic feed failures and data integrity is not in question. Required approvals: reviewed source repair, deployment/reconciliation and observability/image repair, immutable configuration repair, migration, rollback, and restore.

## Current verification update — 2026-08-31T11:17:16Z
- Incident remains active as degraded/partial-failure ingestion, not an availability outage. PostgreSQL reports 8 failed ingestion runs, 0 completed runs, and 0 running runs. Latest run `6dc53c71-19c1-5f5b-9fc9-3f33a5064d3d` was scheduled for `02:00Z`, started `02:20:22.326735Z`, completed `02:20:24.470088Z`, and recorded 14 total / 12 successful / 2 failed / 1,840 new documents. No fully successful run exists.
- Latest schema-correct source evidence is unchanged: `google-threat-analysis-group` failed with `http_error`/HTTP 404; `microsoft-security-response-center` failed with `malformed_xml`; all other 12 sources completed with HTTP 200. This remains a high-confidence stale/incompatible enabled-feed cause; failed evidence is retained.
- Runtime is healthy: web, scheduler, monitor, backup, and PostgreSQL are running/healthy; worker exited 0 by reserved-entrypoint design and is expected unhealthy under `restart: no`. Restart counts are web 0, scheduler 0, backup 0, PostgreSQL 0, monitor 3 historical, worker 0; all report `OOMKilled=false`. Scheduler heartbeat was fresh at `2026-08-31T11:16:54Z`. Public `:9443/health/live`, private `:9444/health/ready`, and private `/version` returned HTTP 200. Split-port 404s are expected; Nginx is active with no observed reload/restart.
- PostgreSQL 16.14 is accepting connections; `pg_is_in_recovery=false`, 7 sessions observed, 0 ungranted locks, and Alembic revision `0014_hunt_playbooks` matches the local head. Counts are 1,900 source documents, 96 raw artifacts, 10,554 entity-evidence rows, 3,523 indicators, 1,784 vulnerabilities, 178 reports, 193 report versions, and 193 publications. Orphan non-null entity-evidence references and orphan current-report-version references are both 0. No pending/failed migration or data-integrity evidence was found.
- Host capacity is healthy: root filesystem 38% used with 302 GiB available, 58 GiB memory available, and system FD limit 4,096. Latest encrypted backup `/backups/hermes-20260831T022021Z.dump.enc` is mode 600, 3,110,832 bytes, completed `2026-08-31T02:20:22Z`, and its SHA-256 `80e248284372b5ca7072e6ccb4a4e0888f950ee4414f2299fb04322901e31574` matches `latest.metadata`. `pg_restore --list` cannot validate this encrypted artifact without the backup decryption step; no restore rehearsal or backup mutation was performed. TLS validates for `matrix-1.taild27e3c.ts.net`, Let's Encrypt YE1, through `2026-11-16T14:30:36Z`.
- Release/configuration identity remains repository `main` at HEAD `5f9e17596af08114556cad8353066ce813f8032c`, application `0.1.0`, mutable `cti-hermes:local` image content digest `sha256:fd967d62e79b97bf194c4a5b6f3c3505aa275bf818e4678e87bdf723a2643f45`. Application services were recreated around `02:20Z` on 2026-08-31 while PostgreSQL was not recreated; retained Docker events do not identify the initiator. No approved compatible rollback target or deployment record exists. Deployed env remains mode 600, mtime `2026-08-25T17:20:29-05:00`, and omits `HERMES_SECRET_DIR`, so checked-in Compose validation requires protected runtime interpolation.
- Separate high-confidence observability/deployment drift remains: checked-out source defines authenticated `/api/v1/ops/*` routes, while direct local probes to `/api/v1/ops/last-success` return application 404; internal authenticated monitor probes pass. Unprivileged `nginx -t` remains blocked by the root-only private key; no reload attempted. GitHub issue creation remains blocked because `gh auth status` reports no authenticated host; this repository-local record is the durable incident/maintenance issue.
- Diagnosis-only verification and this incident-record update were performed. Smallest reversible action remains no action: a restart cannot repair deterministic upstream failures, and data integrity is not in question. No restart, migration, deployment, rollback, restore, source edit, credential change, proxy reload, Docker prune, volume deletion, or publication mutation was performed. Required approvals: reviewed source repair, observability/image reconciliation, immutable deployment/configuration repair, migration, rollback, and restore.

## Current verification update — 2026-08-31T12:16Z
- Incident remains active as degraded/partial-failure ingestion, not an availability outage. PostgreSQL reports 8 failed ingestion runs, 0 completed runs, and 0 running runs. Latest run `6dc53c71-19c1-5f5b-9fc9-3f33a5064d3d` was scheduled for `02:00Z`, started `02:20:22.326735Z`, completed `02:20:24.470088Z`, and recorded 14 total / 12 successful / 2 failed / 1,840 new documents; no fully successful run exists.
- Latest source-run rows still show exactly two failures: Google Threat Analysis Group `http_error`/HTTP 404 and Microsoft Security Response Center `malformed_xml`; all other 12 sources completed with HTTP 200. Direct probes at approximately `12:16Z` reproduce Google `https://blog.google/threat-analysis-group/rss/` as HTTP/2 404 `text/html` (227,758 bytes), and MSRC `https://msrc.microsoft.com/blog/feed` as HTTP/2 301 to `https://www.microsoft.com/en-us/msrc/blog`, followed by HTTP/2 200 `text/html` (94,397 bytes), not RSS/XML. Cause confidence remains high: two stale/incompatible enabled upstream endpoints; failed evidence is retained.
- Runtime is healthy: web, scheduler, monitor, backup, and PostgreSQL are running/healthy; worker exited 0 by reserved-entrypoint design (`restart: no`). Restart counts are web 0, scheduler 0, backup 0, PostgreSQL 0, monitor 3 historical, worker 0; all inspected CTI containers report `OOMKilled=false`. Scheduler heartbeat was `2026-08-31T12:17:25Z`; Docker events in the observation window were diagnostic/healthcheck execs only after the earlier unplanned 02:20Z recreation. Public `:9443/health/live`, private `:9444/health/ready`, and private `/version` returned HTTP 200. No maintenance restart was issued.
- PostgreSQL 16.14 accepts connections, `pg_is_in_recovery=false`, 7 sessions were observed, and there are 0 ungranted locks. Alembic revision is `0014_hunt_playbooks`; the checked-in Compose declares this as the local head. Counts are 1,900 source documents, 96 raw artifacts, 10,554 entity-evidence rows, 3,523 indicators, 1,784 vulnerabilities, 178 reports, 193 report versions, and 193 publications. Schema-correct orphan checks are 0 for entity-evidence/source-document and report/current-version references. No pending/failed migration or data-integrity evidence was found.
- Host capacity is healthy: root filesystem 38% used with 302 GiB available, 58 GiB available memory, load `0.11 0.08 0.08`, FD limit 4,096 with 259 allocated descriptors observed, and no readable kernel OOM evidence. Backup `/backups/hermes-20260831T022021Z.dump.enc` is mode 600, 3,110,832 bytes, completed `2026-08-31T02:20:22Z`; recomputed SHA-256 `80e248284372b5ca7072e6ccb4a4e0888f950ee4414f2299fb04322901e31574` matches `latest.metadata`. No restore rehearsal or backup mutation was performed.
- TLS hostname/chain validation succeeds for `matrix-1.taild27e3c.ts.net`, issued by Let's Encrypt YE1, valid through `2026-11-16T14:30:36Z`. Nginx is active with zero restarts and ingress works; unprivileged `nginx -t` remains blocked by root-only `/etc/nginx/ssl/tailscale/hermes.key`, so no reload was attempted. Monitor logs still report failure for public liveness/private readiness/last-success despite direct health probes passing, indicating an observability/proxy or monitor-route mismatch separate from the source failures.
- Release/configuration identity is repository `git@github.com:mrcoggsworth/threat-intel-agent.git`, checkout branch `fix/optional-provider-gates-and-ioc-scope` with working tree changes (`README.md`, `src/hermes_cti/enrichment/providers.py`, `src/hermes_cti/portal/routes.py`, `tests/test_phase5.py`, and this incident record); `origin/main` and local `main` point to HEAD `5f9e17596af08114556cad8353066ce813f8032c`. Application version is `0.1.0`; running application image is mutable `cti-hermes:local`, content digest `sha256:fd967d62e79b97bf194c4a5b6f3c3505aa275bf818e4678e87bdf723a2643f45`, created `2026-08-30T21:19:59-05:00`. Five application services were recreated around `02:20Z` on 2026-08-31 while PostgreSQL was not recreated; retained events do not identify the initiator. Deployed env remains mode 600, 870 bytes, mtime `2026-08-25T17:20:29-05:00`; it omits `HERMES_SECRET_DIR`, so Compose validation requires protected explicit interpolation. No deployment record or approved compatible rollback target exists.
- GitHub issue creation remains blocked because `gh auth status` reports no authenticated host; this repository-local incident record is the durable maintenance issue. Smallest reversible action remains no action: deterministic source failures are isolated, availability and data integrity are healthy, and restart/rollback would not repair them. No restart, migration, deployment, rollback, restore, source edit, credential change, proxy reload, Docker prune, volume deletion, or publication mutation was performed. Required approvals: reviewed source-registry repair, reconciliation/deployment correlation or repair, observability/monitor-route and immutable-image/configuration repair, migration, rollback, and restore.

## Current verification update — 2026-08-31T13:02Z
- Incident remains active as degraded/partial-failure ingestion, not an availability outage. PostgreSQL reports 8 failed ingestion runs, 0 completed runs, and 0 running runs. Latest run `6dc53c71-19c1-5f5b-9fc9-3f33a5064d3d` was scheduled 02:00Z, started 02:20:22.326735Z, completed 02:20:24.470088Z, and recorded 14 total / 12 successful / 2 failed / 1,840 new documents; no fully successful run exists.
- Exact upstream probes at 13:02Z reproduce the same two failures: Google TAG returns HTTP/2 404 `text/html` (227,758 bytes); MSRC returns HTTP/2 301 to `https://www.microsoft.com/en-us/msrc/blog` and then HTML (94,397 bytes), not RSS/XML. The other 12 latest source rows completed with HTTP 200. Primary cause confidence remains high: stale/incompatible enabled feed endpoints; failed evidence is retained.
- Runtime is healthy and stable: web, scheduler, monitor, backup, and PostgreSQL are running/healthy; worker exited 0 by reserved-entrypoint design (`restart: no`, expected unhealthy). Restart counts are web 0, scheduler 0, backup 0, PostgreSQL 0, monitor 3 historical, worker 0; application containers report `OOMKilled=false`. Public `:9443/health/live`, private `:9444/health/ready`, and private `/version` returned HTTP 200. Scheduler heartbeat was fresh at 13:00:55Z; Docker events showed healthcheck/diagnostic execs only.
- PostgreSQL 16.14 accepts connections, `pg_is_in_recovery=false`, with 12 sessions and 0 ungranted locks. Alembic revision `0014_hunt_playbooks` matches local head. Counts are 1,900 source documents, 96 raw artifacts, 10,554 entity-evidence rows, 3,523 indicators, 1,784 vulnerabilities, 178 reports, 193 report versions, and 193 publications; orphan non-null entity-evidence and orphan current-report-version references are both 0. No pending/failed migration or integrity evidence was found.
- Host capacity is healthy: root filesystem 38% used with 302 GiB available, 58 GiB memory available, FD limit 4,096, and no observed container OOM signal. Latest encrypted backup `/backups/hermes-20260831T022021Z.dump.enc` is mode 600, 3,110,832 bytes, completed 02:20:22Z; recomputed SHA-256 `80e248284372b5ca7072e6ccb4a4e0888f950ee4414f2299fb04322901e31574` matches `latest.metadata`. No restore rehearsal or backup mutation was performed. TLS hostname/chain verification succeeds for `matrix-1.taild27e3c.ts.net`, Let's Encrypt YE1, through 2026-11-16T14:30:36Z.
- Release/configuration identity remains repository checkout branch `fix/optional-provider-gates-and-ioc-scope` at HEAD `5f9e17596af08114556cad8353066ce813f8032c`, app `0.1.0`, mutable `cti-hermes:local` image content digest `sha256:fd967d62e79b97bf194c4a5b6f3c3505aa275bf818e4678e87bdf723a2643f45`. Application services were recreated around 02:20Z on 2026-08-31 while PostgreSQL was not; no initiator is identified in retained events. Deployed env remains mode 600, mtime 2026-08-25T17:20:29-05:00, and omits required `HERMES_SECRET_DIR`; checked-in Compose validation is blocked without protected interpolation. No approved compatible rollback target or deployment record exists.
537|- Separate observability/deployment drift remains: monitor logs report public liveness/private readiness/last-success failure despite direct health probes passing; the worker's intentional exited state is expected. Unprivileged `nginx -t` remains blocked by the root-only private key; no reload was attempted. GitHub issue creation remains blocked because `gh auth status` reports no authenticated host; this local record is the durable incident/maintenance issue.
538|- Diagnosis-only verification and this incident-record update were performed. Smallest reversible action remains no action: restarting or rolling back cannot repair deterministic upstream failures, and data integrity is not in question. No restart, migration, deployment, rollback, restore, source edit, credential change, proxy reload, Docker prune, volume deletion, or publication mutation was performed. Required approvals: reviewed source repair, deployment/recreation reconciliation, observability/monitor-route and immutable-image/configuration repair, migration, rollback, and restore.

## Current verification update — 2026-08-31T13:19:00Z
- Incident remains active as degraded/partial-failure ingestion, not an availability outage. Live PostgreSQL state is 8 failed runs, 0 completed runs, and 0 running runs. Latest run `6dc53c71-19c1-5f5b-9fc9-3f33a5064d3d` is failed (14 total / 12 successful / 2 failed / 1,840 new documents), completed `2026-08-31T02:20:24.470088Z`; no fully successful run exists.
- Exact latest source-run rows still show Google Threat Analysis Group `http_error`/HTTP 404 and Microsoft Security Response Center `malformed_xml`; all other 12 enabled sources completed with HTTP 200. Direct probes at 13:17Z reproduce Google TAG HTTP/2 404 HTML and MSRC HTTP/2 301 followed by HTTP/2 200 HTML, not RSS/XML. Primary cause confidence remains high: two stale/incompatible enabled endpoints; failed evidence is retained.
- Runtime is healthy: web, scheduler, monitor, backup, and PostgreSQL are running/healthy; worker exited 0 by reserved-entrypoint design. Restart counts are web 0, scheduler 0, backup 0, PostgreSQL 0, monitor 3 historical, worker 0; all inspected application containers report `OOMKilled=false`. Heartbeat is fresh at `2026-08-31T13:17:25Z`; monitor one-shot exited 0. Public `/health/live` on 9443 and private `/health/ready` plus `/version` on 9444 returned HTTP 200. Docker events since the 02:20Z reconciliation show healthcheck/diagnostic execs only; no new lifecycle restart/crash event.
- PostgreSQL 16.14 accepts connections, `pg_is_in_recovery=false`, 12 sessions observed against max 100, 0 ungranted locks, and Alembic `0014_hunt_playbooks` matches local head. Schema-correct orphan entity-evidence and orphan current-report-version checks are both 0; counts remain 1,900 source documents, 96 raw artifacts, 10,554 entity-evidence rows, 3,523 indicators, 1,784 vulnerabilities, 178 reports, 193 report versions, and 193 publications. No pending/failed migration or data-integrity evidence was found.
- Host capacity is healthy: root filesystem 38% used with 302 GiB available, 58 GiB memory available, load `0.25 0.16 0.10`, system FD limit 4,096, and 2,784 allocated files; no container OOM signal. Latest encrypted backup `/backups/hermes-20260831T022021Z.dump.enc` is mode 600, 3,110,832 bytes, completed `2026-08-31T02:20:22Z`, with SHA-256 `80e248284372b5ca7072e6ccb4a4e0888f950ee4414f2299fb04322901e31574` matching metadata. No restore rehearsal or backup mutation was performed.
- TLS hostname/chain validation succeeds for `matrix-1.taild27e3c.ts.net`, Let's Encrypt YE1, through `2026-11-16T14:30:36Z`. Nginx is active with zero restarts and ingress works; unprivileged `nginx -t` remains blocked by the root-only private key and passwordless sudo is unavailable, so no reload was attempted.
- Release/configuration identity remains repository checkout branch `fix/optional-provider-gates-and-ioc-scope` at HEAD `5f9e17596af08114556cad8353066ce813f8032c`, application `0.1.0`, mutable `cti-hermes:local` image digest `sha256:fd967d62e79b97bf194c4a5b6f3c3505aa275bf818e4678e87bdf723a2643f45`. Application services were recreated around 02:20Z while PostgreSQL was not; initiator remains unidentified. Deployed env is mode 600, mtime `2026-08-25T17:20:29-05:00`, and omits `HERMES_SECRET_DIR`; Compose validates only with protected explicit interpolation. No deployment record or approved compatible rollback target exists. External GitHub issue creation remains blocked by unauthenticated `gh`; this local incident record is the durable maintenance issue.
548|- Separate high-confidence observability/deployment drift remains: internal monitor probes pass, but the supplied private base URL's public-only paths return expected split-port 404s; Nginx config validation remains unavailable without privileged access. Smallest reversible action remains no action: restart/rollback would not repair deterministic feed failures, and service/data integrity is healthy. No restart, migration, deployment, rollback, restore, source edit, credential change, proxy reload, Docker prune, volume deletion, or publication mutation was performed. Required approvals: reviewed source repair, observability/monitor-route and immutable-image/configuration repair, migration, rollback, and restore.

## Current verification update — 2026-08-31T09:05:00-05:00 (14:05Z)
- Incident remains active as degraded/partial-failure ingestion, not an availability outage. Live PostgreSQL state is 8 failed runs, 0 completed runs, and 0 running runs. Latest run `6dc53c71-19c1-5f5b-9fc9-3f33a5064d3d` was scheduled 02:00Z, ran 02:20:22.326735Z–02:20:24.470088Z, and recorded 14 total / 12 successful / 2 failed / 1,840 new documents. No fully completed run exists; the latest partial-success timestamp is 02:20:24.470088Z.
- Exact configured source evidence remains reproducible: Google TAG returns HTTP/2 404 `text/html` (227,758 bytes); MSRC returns HTTP/2 301 to `https://www.microsoft.com/en-us/msrc/blog`, followed by HTTP/2 200 HTML (94,397 bytes), not RSS/XML. Latest `source_run` rows classify these as `http_error`/404 and `malformed_xml`; the other 12 sources completed with HTTP 200. Primary cause confidence remains high: two stale/incompatible enabled feed endpoints; failed evidence is retained.
- Runtime is healthy: web, scheduler, monitor, backup, and PostgreSQL are running/healthy; worker exited 0 by reserved-entrypoint design and is expected unhealthy under `restart: no`. Restart counts are web 0, scheduler 0, backup 0, PostgreSQL 0, monitor 3 historical, worker 0; application containers report `OOMKilled=false`. Scheduler heartbeat was fresh at capture, and recent scheduler logs contain only the generic source-collection failure message. Correctly routed probes returned public `:9443/health/live` 200, private `:9444/health/ready` 200 (`configuration=ok`, `database=ok`), and private `/version` 200 (`0.1.0`).
- PostgreSQL 16.14 accepts connections, `pg_is_in_recovery=false`, with 7 sessions of 100 and 0 ungranted locks. Alembic is `0014_hunt_playbooks`, matching the local head. Counts are 1,900 source documents, 96 raw artifacts, 10,554 entity-evidence rows, 3,523 indicators, 1,784 vulnerabilities, 178 reports, 193 report versions, and 193 publications. Orphan non-null entity-evidence references and orphan current-report-version references are both 0. No pending/failed migration or data-integrity evidence was found; PostgreSQL errors in the observation window were malformed diagnostic probes or invalid-role probes, not application failures.
- Host capacity is healthy: root filesystem 38% used with 301 GiB available, 55 GiB memory available, load 0.23/0.36/0.22, system FD limit 4,096 with 3,616 allocated files, and no container OOM signal. Latest encrypted backup `/backups/hermes-20260831T022021Z.dump.enc` is mode 600, 3,110,832 bytes, completed 02:20:22Z; SHA-256 `80e248284372b5ca7072e6ccb4a4e0888f950ee4414f2299fb04322901e31574` matches `latest.metadata`. No restore rehearsal or backup mutation was performed.
- TLS hostname/chain validation succeeds for `matrix-1.taild27e3c.ts.net`, issued by Let's Encrypt YE1, through `2026-11-16T14:30:36Z`. Nginx is active with zero restarts and ingress works, but unprivileged `nginx -t` cannot read root-only `/etc/nginx/ssl/tailscale/hermes.key`; passwordless sudo is unavailable, so no reload was attempted.
- Release/configuration identity is repository checkout branch `fix/optional-provider-gates-and-ioc-scope` at HEAD `5f9e17596af08114556cad8353066ce813f8032c`, with pre-existing tracked/untracked working-tree changes. Running application image is mutable `cti-hermes:local`, content digest `sha256:fd967d62e79b97bf194c4a5b6f3c3505aa275bf818e4678e87bdf723a2643f45`, application `0.1.0`; application services were recreated around 02:20Z on 2026-08-31 while PostgreSQL was not, and retained events do not identify the initiator. Deployed env remains mode 600, mtime `2026-08-25T17:20:29-05:00`, and omits required `HERMES_SECRET_DIR`; Compose validates only with protected explicit interpolation. No deployment record or approved compatible rollback target exists.
- Separate observability/deployment drift remains high confidence: direct external probes to the supplied private base URL return expected split-port 404s for public-only paths, while internal authenticated monitor probes pass. Nginx validation remains privilege-blocked. Diagnosis-only verification was performed; no restart, migration, deployment, rollback, restore, source edit, credential change, proxy reload, Docker prune, volume deletion, or publication mutation occurred. External GitHub issue creation remains blocked by unauthenticated `gh`; this local incident record is the durable maintenance handoff. Required approvals remain reviewed source repair, reconciliation of the unrecorded recreation, observability/image/configuration repair, migration, rollback, and restore.

## Current verification update — 2026-08-31T09:48:02-05:00 (14:48Z)
- Incident remains active as degraded/partial-failure ingestion, not an availability outage. Live PostgreSQL state is 8 failed runs, 0 completed runs, and 0 running runs. Latest run `6dc53c71-19c1-5f5b-9fc9-3f33a5064d3d` was scheduled for 02:00Z, started 02:20:22.326735Z, completed 02:20:24.470088Z, and recorded 14 total / 12 successful / 2 failed / 1,840 new documents. No fully successful run exists; the latest partial-success timestamp is 02:20:24.470088Z.
- Exact authoritative upstream probes remain reproducible: Google TAG returns HTTP/2 404 `text/html` (227,758 bytes); MSRC returns HTTP/2 301 to `https://www.microsoft.com/en-us/msrc/blog`, then HTTP/2 200 `text/html` (94,397 bytes), not RSS/XML. Latest source rows classify these as `http_error`/404 and `malformed_xml`; all other 12 sources completed with HTTP 200. Primary cause confidence remains high: two stale/incompatible enabled feed endpoints; failed evidence is retained.
- Runtime is healthy: web, scheduler, monitor, backup, and PostgreSQL are running/healthy; worker exited 0 by reserved-entrypoint design and is expected unhealthy under `restart: no`. Restart counts are web 0, scheduler 0, backup 0, PostgreSQL 0, monitor 3 historical, worker 0; inspected application containers report `OOMKilled=false`. Scheduler heartbeat file is fresh at `2026-08-31T14:47:56Z`; public `:9443/health/live`, private `:9444/health/ready`, and private `/version` all returned HTTP 200. Nginx is active with no observed restart; its unprivileged config test remains blocked by the root-only private key, so no reload was attempted.
- PostgreSQL 16.14 accepts connections, `pg_is_in_recovery=false`, revision is `0014_hunt_playbooks` matching local head, and no pending/failed migration or ungranted-lock evidence was found. Schema-correct orphan checks remain 0 for non-null entity-evidence/source-document and report/current-version references. Current connection count was 12 of 100; data counts include 1,900 source documents, 96 raw artifacts, 10,554 entity-evidence rows, 3,523 indicators, 1,784 vulnerabilities, 178 reports, 193 report versions, and 193 publications.
- Host capacity is healthy: root filesystem 38% used with 301 GiB available, about 55 GiB memory available, load low, FD limit 4,096, and no container OOM signal. Latest encrypted backup `/backups/hermes-20260831T022021Z.dump.enc` is mode 600, 3,110,832 bytes, completed 02:20:22Z; SHA-256 `80e248284372b5ca7072e6ccb4a4e0888f950ee4414f2299fb04322901e31574` matches metadata. No restore rehearsal or backup mutation was performed. TLS validates for `matrix-1.taild27e3c.ts.net`, Let's Encrypt YE1, through `2026-11-16T14:30:36Z`.
- Current repository is clean on branch `feat/cve-page-navigation-and-feeds` at HEAD `c1b2d4c7448f19db8d7f13b9a81383a28179272f`; local/origin `main` remain `5f9e17596af08114556cad8353066ce813f8032c`. Running application remains mutable `cti-hermes:local`, image content ID `sha256:fd967d62e79b97bf194c4a5b6f3c3505aa275bf818e4678e87bdf723a2643f45`, application `0.1.0`; image was created 2026-08-30T21:19:59-05:00. Five application services were recreated around 02:20Z on 2026-08-31 while PostgreSQL was not; retained events do not identify the initiator. Deployed env remains mode 600, mtime 2026-08-25T17:20:29-05:00, and omits required `HERMES_SECRET_DIR`, so checked-in Compose validation fails without protected interpolation. No deployment record or approved compatible rollback target exists.
- Separate high-confidence observability/deployment drift remains: monitor one-shot logs report probe failure during startup ordering, but current internal authenticated probes and direct external health/version probes pass. GitHub issue creation remains blocked by unauthenticated `gh`; this repository-local incident record is the durable maintenance issue. Smallest reversible action remains no action: restart/rollback cannot repair deterministic upstream failures and data integrity is healthy. No restart, migration, deployment, rollback, restore, source edit, credential change, proxy reload, Docker prune, volume deletion, or publication mutation was performed. Required approvals: reviewed source repair, reconciliation of the unrecorded recreation, immutable image/configuration and observability repair, migration, rollback, and restore.
